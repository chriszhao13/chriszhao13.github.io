<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://chriszhao13.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://chriszhao13.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-23T09:48:29+00:00</updated><id>https://chriszhao13.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">我所理解的 CUDA · 第 4 章：Work Stealing with Cluster Launch Control</title><link href="https://chriszhao13.github.io/blog/2026/cluster-launch-control/" rel="alternate" type="text/html" title="我所理解的 CUDA · 第 4 章：Work Stealing with Cluster Launch Control"/><published>2026-09-23T09:00:00+00:00</published><updated>2026-09-23T09:00:00+00:00</updated><id>https://chriszhao13.github.io/blog/2026/cluster-launch-control</id><content type="html" xml:base="https://chriszhao13.github.io/blog/2026/cluster-launch-control/"><![CDATA[<blockquote> <p>本节对应《CUDA C++ Programming Guide》Release 13.3 <strong>第 4 章 CUDA Features，§4.12 Work Stealing with Cluster Launch Control</strong>。</p> </blockquote> <p><strong>本文包含两部分：</strong></p> <table> <thead> <tr> <th>部分</th> <th>内容</th> </tr> </thead> <tbody> <tr> <td>文档中英对照</td> <td>官方 §4.12 全文翻译，术语与 API 细节以官方文档为准</td> </tr> <tr> <td>原理与实测</td> <td>工作窃取原理剖析、<strong>可直接编译运行的完整代码</strong>、Blackwell（sm_120）实测复现</td> </tr> </tbody> </table> <p>「原理与实测」部分给出<strong>一份完整、可直接编译运行的 <code class="language-plaintext highlighter-rouge">.cu</code> 源码</strong>（无需任何额外依赖），并从三个问题出发讲清原理：<strong>为什么能省 prologue、为什么能自然负载均衡、循环靠什么终止</strong>。运行方式与预期输出见文末「复现」一节。</p> <h2 id="412-work-stealing-with-cluster-launch-control基于簇启动控制的工作窃取">4.12 Work Stealing with Cluster Launch Control（基于簇启动控制的工作窃取）</h2> <blockquote> <p>Dealing with problems of variable data and computation sizes is essential when developing CUDA applications. Traditionally, CUDA developers have used two main approaches to determine the number of kernel thread blocks to launch: <em>fixed work per thread block</em> and <em>fixed number of thread blocks</em>. Both approaches have their advantages and disadvantages.</p> </blockquote> <p>在开发 CUDA 应用时，如何处理<strong>数据规模与计算量可变</strong>的问题至关重要。传统上，CUDA 开发者使用两种主要方法来确定内核启动的线程块数量：<strong>固定每线程块工作量（fixed work per thread block）</strong>与<strong>固定线程块数量（fixed number of thread blocks）</strong>。这两种方法各有优劣。</p> <blockquote> <p><strong>Fixed Work per Thread Block:</strong> In this approach, the number of thread blocks is determined by the problem size, while the amount of work done by each thread block remains constant.</p> </blockquote> <p><strong>固定每线程块工作量（Fixed Work per Thread Block）</strong>：在这种方法中，线程块的数量由问题规模决定，而每个线程块完成的工作量保持不变。</p> <blockquote> <p>Key advantages of this approach:</p> </blockquote> <p>该方法的主要优点：</p> <blockquote> <ul> <li><em>Load balancing between SMs</em></li> </ul> <p>When thread block run-times exhibit variability and/or when the number of thread blocks is much larger than what the GPU can execute simultaneously (resulting in a low-tail effect), this approach allows the GPU scheduler to run more thread blocks on some SMs than others.</p> </blockquote> <ul> <li> <p><strong>SM 之间的负载均衡</strong></p> <p>当线程块的运行时间存在波动，和/或线程块数量远大于 GPU 能同时执行的数量时（从而产生<strong>长尾效应，low-tail effect</strong>），该方法允许 GPU 调度器在某些 SM 上运行比其他 SM 更多的线程块。</p> </li> </ul> <blockquote> <ul> <li><em>Preemption</em></li> </ul> <p>The GPU scheduler can start executing a <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/asynchronous-execution.html#async-execution-stream-priorities">higher-priority kernel</a>, even if it is launched after a lower-priority kernel has already begun executing, by scheduling its thread blocks as thread blocks of the lower-priority kernel complete. It can then resume execution of the lower-priority kernel once the higher-priority kernel has finished executing.</p> </blockquote> <ul> <li> <p><strong>抢占（Preemption）</strong></p> <p>GPU 调度器可以开始执行<strong>更高优先级的内核</strong>（<a href="https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/asynchronous-execution.html#async-execution-stream-priorities">higher-priority kernel</a>），即使该内核是在低优先级内核已经开始执行之后才启动的 —— 它会在低优先级内核的线程块完成时，把高优先级内核的线程块调度上去。等高优先级内核执行完毕后，调度器再恢复低优先级内核的执行。</p> </li> </ul> <blockquote> <p><strong>Fixed Number of Thread Blocks:</strong> In this approach, often implemented as a block-stride or grid-stride loop, the number of thread blocks does not depend on the problem size. Instead, the amount of work done by each thread block is a function of the problem size. Typically, the number of thread blocks is based on the number of SMs on the GPU where the kernel is executed and the desired occupancy.</p> </blockquote> <p><strong>固定线程块数量（Fixed Number of Thread Blocks）</strong>：这种方法通常以 <strong>block-stride 循环</strong>或 <strong>grid-stride 循环</strong>实现，线程块数量不依赖于问题规模；相反，每个线程块完成的工作量是问题规模的函数。通常，线程块数量取决于执行该内核的 GPU 上的 SM 数量以及期望的<strong>占用率（occupancy）</strong>。</p> <blockquote> <p>Key advantages of this approach:</p> </blockquote> <p>该方法的主要优点：</p> <blockquote> <ul> <li><em>Reduced thread block overheads</em></li> </ul> <p>This approach not only reduces amortized thread block launch latency but also minimizes the computational overhead associated with shared operations across all thread blocks. These overheads can be significantly higher than launch latency overheads.</p> </blockquote> <ul> <li> <p><strong>降低线程块开销</strong></p> <p>该方法不仅降低了分摊后的线程块启动延迟（amortized thread block launch latency），还最小化了所有线程块之间共享操作（shared operations）带来的计算开销。这些开销可能显著高于启动延迟本身。</p> </li> </ul> <blockquote> <p>For example, in convolution kernels, a prologue for calculating convolution coefficients – independent of the thread block index – can be computed fewer times due to the fixed number of thread blocks, thus reducing redundant computations.</p> </blockquote> <p>例如，在卷积内核中，与线程块索引无关的<strong>前导部分（prologue）</strong>（用于计算卷积系数）由于线程块数量固定，可以被计算的次数更少，从而减少冗余计算。</p> <blockquote> <p><strong>Cluster Launch Control</strong> is a feature introduced in the NVIDIA Blackwell GPU architecture (compute capability 10.0) that aims to combine the benefits of the previous two approaches. It provides developers with more control over thread block scheduling by allowing them to cancel thread blocks or thread block clusters. This mechanism enables work stealing. Work stealing is a dynamic load-balancing technique in parallel computing where idle processors actively “steal” tasks from the work queues of busy processors, rather than wait for work to be assigned.</p> </blockquote> <p><strong>簇启动控制（Cluster Launch Control）</strong> 是 NVIDIA Blackwell GPU 架构（计算能力 10.0）引入的一项特性，旨在<strong>同时结合前两种方法的优点</strong>。它让开发者对线程块调度拥有更多控制权 —— 允许取消线程块或线程块簇。该机制实现了<strong>工作窃取（work stealing）</strong>。工作窃取是并行计算中的一种<strong>动态负载均衡技术</strong>：空闲的处理器主动从繁忙处理器的工作队列中”窃取”任务，而不是被动等待任务被分配。</p> <blockquote> <p>图 51（Cluster Launch Control Flow，簇启动控制流程）请参见官方文档配图：<a href="https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cluster-launch-control.html#cluster-launch-control-diagram">Figure 51 Cluster Launch Control Flow</a>。</p> </blockquote> <blockquote> <p>With cluster launch control, a thread block attempts to cancel the launch of another thread block that has not started executing yet. If the cancellation request succeeds, it “steals” the other thread block’s work by using its index to perform the task. The cancellation will fail if there are no more thread block indices available or for other reasons, such as a higher-priority kernel being scheduled. In the latter case, if a thread block exits after a cancellation failure, the scheduler can start executing the higher-priority kernel, after which it will continue scheduling the remaining thread blocks of the current kernel for execution.</p> </blockquote> <p>使用簇启动控制时，一个线程块会尝试<strong>取消另一个尚未开始执行的线程块的启动</strong>。如果取消请求成功，它就通过使用对方的索引来执行任务，从而”窃取”了那个线程块的工作。如果已经没有更多可用的线程块索引，或者出于其他原因（例如有更高优先级的内核被调度），取消就会失败。在后一种情况下，如果线程块在取消失败后退出，调度器就可以开始执行更高优先级的内核；之后它会继续调度当前内核剩余的线程块执行。</p> <blockquote> <p>The table below summarizes advantages and disadvantages of the three approaches:</p> </blockquote> <p>下表总结了这三种方法的优缺点：</p> <table> <thead> <tr> <th>特性</th> <th>固定每线程块工作量</th> <th>固定线程块数量</th> <th>簇启动控制</th> </tr> </thead> <tbody> <tr> <td>降低开销</td> <td>✗</td> <td>✓</td> <td>✓</td> </tr> <tr> <td>抢占</td> <td>✓</td> <td>✗</td> <td>✓</td> </tr> <tr> <td>负载均衡</td> <td>✓</td> <td>✗</td> <td>✓</td> </tr> </tbody> </table> <hr/> <h2 id="4121-api-detailsapi-细节">4.12.1 API Details（API 细节）</h2> <blockquote> <p>Cancelling a thread block via the cluster launch control API is done asynchronously and synchronized using a shared memory barrier, following a programming pattern similar to <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/03-advanced/advanced-kernel-programming.html#advanced-kernels-async-copies">asynchronous data copies</a>.</p> </blockquote> <p>通过簇启动控制 API 取消线程块是<strong>异步</strong>完成的，并使用<strong>共享内存屏障</strong>进行同步，其编程模式与<a href="https://docs.nvidia.com/cuda/cuda-programming-guide/03-advanced/advanced-kernel-programming.html#advanced-kernels-async-copies">异步数据拷贝</a>类似。</p> <blockquote> <p>The API, available through <a href="https://nvidia.github.io/cccl/libcudacxx/ptx_api.html">libcu++</a>, provides:</p> </blockquote> <p>该 API 通过 <a href="https://nvidia.github.io/cccl/libcudacxx/ptx_api.html">libcu++</a> 提供，包含：</p> <blockquote> <ul> <li>A request instruction that writes encoded cancellation results to a <code class="language-plaintext highlighter-rouge">__shared__</code> variable.</li> <li>Decoding instructions that extract success/failure status and the cancelled thread block index.</li> </ul> </blockquote> <ul> <li>一条<strong>请求指令</strong>，将编码后的取消结果写入一个 <code class="language-plaintext highlighter-rouge">__shared__</code> 变量；</li> <li>若干<strong>解码指令</strong>，用于提取成功/失败状态以及被取消的线程块索引。</li> </ul> <blockquote> <p>Note that cluster launch control operations are modeled as async proxy operations (see <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/03-advanced/advanced-kernel-programming.html#advanced-kernels-hardware-implementation-asynchronous-execution-features-async-thread-proxy">Async Thread and Async Proxy</a>).</p> </blockquote> <p>注意：簇启动控制操作被建模为 <strong>async proxy 操作</strong>（参见 <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/03-advanced/advanced-kernel-programming.html#advanced-kernels-hardware-implementation-asynchronous-execution-features-async-thread-proxy">Async Thread and Async Proxy</a>）。</p> <h3 id="41211-thread-block-cancellation线程块取消">4.12.1.1 Thread Block Cancellation（线程块取消）</h3> <blockquote> <p>The preferred way to use Cluster Launch Control is from a single thread, i.e., one request at a time.</p> </blockquote> <p>使用簇启动控制的首选方式是<strong>由单个线程发起</strong>，即一次只发一个请求。</p> <blockquote> <p>The cancellation process involves five steps:</p> </blockquote> <p>取消过程包含五个步骤：</p> <blockquote> <ul> <li><strong>Setup Phase</strong> (Steps 1-2): Declare and initialize cancellation result and synchronization variables.</li> <li><strong>Work-Stealing Loop</strong> (Steps 3-5): Execute repeatedly to request, synchronize, and process cancellation results.</li> </ul> </blockquote> <ul> <li><strong>准备阶段（Setup Phase）</strong>（步骤 1–2）：声明并初始化取消结果变量与同步变量。</li> <li><strong>工作窃取循环（Work-Stealing Loop）</strong>（步骤 3–5）：反复执行，以发起请求、同步并处理取消结果。</li> </ul> <blockquote> <ol> <li>Declare variables for thread block cancellation:</li> </ol> </blockquote> <ol> <li>声明用于线程块取消的变量：</li> </ol> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__shared__</span> <span class="n">uint4</span> <span class="n">result</span><span class="p">;</span>  <span class="c1">// Request result.</span>
<span class="n">__shared__</span> <span class="kt">uint64_t</span> <span class="n">bar</span><span class="p">;</span>  <span class="c1">// Synchronization barrier.</span>
<span class="kt">int</span> <span class="n">phase</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>            <span class="c1">// Synchronization barrier phase.</span>
</code></pre></div></div> <blockquote> <ol> <li>Initialize shared memory barrier with a single arrival count:</li> </ol> </blockquote> <ol> <li>以<strong>到达计数为 1</strong> 初始化共享内存屏障：</li> </ol> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="p">(</span><span class="n">cg</span><span class="o">::</span><span class="n">thread_block</span><span class="o">::</span><span class="n">thread_rank</span><span class="p">()</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span>
    <span class="n">ptx</span><span class="o">::</span><span class="n">mbarrier_init</span><span class="p">(</span><span class="o">&amp;</span><span class="n">bar</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>
<span class="n">__syncthreads</span><span class="p">();</span>
</code></pre></div></div> <blockquote> <ol> <li>Submit asynchronous cancellation request by a single thread and set transaction count:</li> </ol> </blockquote> <ol> <li>由单个线程提交异步取消请求，并设置事务计数（transaction count）：</li> </ol> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="p">(</span><span class="n">cg</span><span class="o">::</span><span class="n">thread_block</span><span class="o">::</span><span class="n">thread_rank</span><span class="p">()</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">cg</span><span class="o">::</span><span class="n">invoke_one</span><span class="p">(</span><span class="n">cg</span><span class="o">::</span><span class="n">coalesced_threads</span><span class="p">(),</span> <span class="p">[</span><span class="o">&amp;</span><span class="p">](){</span>
        <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_try_cancel</span><span class="p">(</span><span class="o">&amp;</span><span class="n">result</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bar</span><span class="p">);</span>
    <span class="p">});</span>
    <span class="n">ptx</span><span class="o">::</span><span class="n">mbarrier_arrive_expect_tx</span><span class="p">(</span>
        <span class="n">ptx</span><span class="o">::</span><span class="n">sem_relaxed</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">scope_cta</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">space_shared</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bar</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">uint4</span><span class="p">));</span>
<span class="p">}</span>
</code></pre></div></div> <blockquote> <p>Note</p> <p>Since thread block cancellation is a uniform instruction, it is recommended to submit it inside <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cooperative-groups.html#cooperative-groups-invoke-one">invoke_one</a> thread selector. This allows the compiler to optimize out the peeling loop.</p> </blockquote> <p><strong>注</strong>：由于线程块取消是一条 <strong>uniform 指令</strong>，建议将其放在 <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cooperative-groups.html#cooperative-groups-invoke-one">invoke_one</a> 线程选择器内部提交。这样编译器可以优化掉剥离循环（peeling loop）。</p> <blockquote> <ol> <li>Synchronize (complete) asynchronous cancellation request:</li> </ol> </blockquote> <ol> <li>同步（等待完成）异步取消请求：</li> </ol> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">ptx</span><span class="o">::</span><span class="n">mbarrier_try_wait_parity</span><span class="p">(</span><span class="o">&amp;</span><span class="n">bar</span><span class="p">,</span> <span class="n">phase</span><span class="p">))</span> <span class="p">{}</span>
<span class="n">phase</span> <span class="o">^=</span> <span class="mi">1</span><span class="p">;</span>
</code></pre></div></div> <blockquote> <ol> <li>Retrieve cancellation status and cancelled thread block index:</li> </ol> </blockquote> <ol> <li>获取取消状态与被取消的线程块索引：</li> </ol> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">bool</span> <span class="n">success</span> <span class="o">=</span> <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_query_cancel_is_canceled</span><span class="p">(</span><span class="n">result</span><span class="p">);</span>
<span class="k">if</span> <span class="p">(</span><span class="n">success</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Don't need all three for 1D/2D thread blocks:</span>
    <span class="kt">int</span> <span class="n">bx</span> <span class="o">=</span> <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_query_cancel_get_first_ctaid_x</span><span class="p">(</span><span class="n">result</span><span class="p">);</span>
    <span class="kt">int</span> <span class="n">by</span> <span class="o">=</span> <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_query_cancel_get_first_ctaid_y</span><span class="p">(</span><span class="n">result</span><span class="p">);</span>
    <span class="kt">int</span> <span class="n">bz</span> <span class="o">=</span> <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_query_cancel_get_first_ctaid_z</span><span class="p">(</span><span class="n">result</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <blockquote> <ol> <li>Ensure visibility of shared memory operations between async and generic <a href="https://docs.nvidia.com/cuda/parallel-thread-execution/index.html#proxies">proxies</a>, and protect against data races between iterations of the work-stealing loop.</li> </ol> </blockquote> <ol> <li>确保共享内存操作在 async proxy 与 generic <a href="https://docs.nvidia.com/cuda/parallel-thread-execution/index.html#proxies">proxy</a> 之间的可见性，并防止工作窃取循环各次迭代之间发生数据竞争。</li> </ol> <h3 id="41212-constraints-on-thread-block-cancellation线程块取消的约束">4.12.1.2 Constraints on Thread Block Cancellation（线程块取消的约束）</h3> <blockquote> <p>The constraints are related to failed cancellation requests:</p> </blockquote> <p>这些约束都与<strong>取消请求失败</strong>有关：</p> <blockquote> <ul> <li>Submitting another cancellation request after <strong>observing</strong> a previously failed request is <em>undefined behavior</em>.</li> </ul> </blockquote> <ul> <li>在<strong>观察到</strong>之前某个请求失败之后，再提交另一个取消请求，属于<strong>未定义行为（undefined behavior）</strong>。</li> </ul> <blockquote> <p>In the two code examples below, assuming the first cancellation request fails, only the first example exhibits undefined behavior. The second example is correct because there is no observation between the cancellation requests:</p> </blockquote> <p>在下面两个代码示例中，假设第一个取消请求失败：只有第一个示例产生未定义行为。第二个示例是正确的，因为两次取消请求之间没有发生”观察”。</p> <p><strong>Invalid code（错误代码）：</strong></p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// First request:</span>
<span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_try_cancel</span><span class="p">(</span><span class="o">&amp;</span><span class="n">result0</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bar0</span><span class="p">);</span>
<span class="c1">// First request query:</span>
<span class="p">[</span><span class="n">Synchronize</span> <span class="n">bar0</span> <span class="n">code</span> <span class="n">here</span><span class="p">.]</span>
<span class="kt">bool</span> <span class="n">success0</span> <span class="o">=</span> <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_query_cancel_is_canceled</span><span class="p">(</span><span class="n">result0</span><span class="p">);</span>
<span class="n">assert</span><span class="p">(</span><span class="o">!</span><span class="n">success0</span><span class="p">);</span>  <span class="c1">// Observed failure; second cancellation will be invalid.</span>
<span class="c1">// Second request - next line is Undefined Behavior:</span>
<span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_try_cancel</span><span class="p">(</span><span class="o">&amp;</span><span class="n">result1</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bar1</span><span class="p">);</span>
</code></pre></div></div> <p><strong>Valid code（正确代码）：</strong></p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// First request:</span>
<span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_try_cancel</span><span class="p">(</span><span class="o">&amp;</span><span class="n">result0</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bar0</span><span class="p">);</span>
<span class="c1">// Second request:</span>
<span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_try_cancel</span><span class="p">(</span><span class="o">&amp;</span><span class="n">result1</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bar1</span><span class="p">);</span>
<span class="c1">// First request query:</span>
<span class="p">[</span><span class="n">Synchronize</span> <span class="n">bar0</span> <span class="n">code</span> <span class="n">here</span><span class="p">.]</span>
<span class="kt">bool</span> <span class="n">success0</span> <span class="o">=</span> <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_query_cancel_is_canceled</span><span class="p">(</span><span class="n">result0</span><span class="p">);</span>
<span class="n">assert</span><span class="p">(</span><span class="o">!</span><span class="n">success0</span><span class="p">);</span>  <span class="c1">// Observed failure; second cancellation was valid.</span>
</code></pre></div></div> <blockquote> <ul> <li>Retrieving the thread block index of a failed cancellation request is Undefined Behavior.</li> </ul> </blockquote> <ul> <li>获取<strong>失败</strong>取消请求的线程块索引，属于<strong>未定义行为</strong>。</li> </ul> <blockquote> <ul> <li>Submitting a cancellation request from multiple threads is not recommended. It results in the cancellation of multiple thread blocks and requires careful handling, such as: <ul> <li>Each submitting thread must provide a unique <code class="language-plaintext highlighter-rouge">__shared__</code> result pointer to avoid data races.</li> <li>If the same barrier is used for synchronization, the arrival and transaction counts must be adjusted accordingly.</li> </ul> </li> </ul> </blockquote> <ul> <li><strong>不建议</strong>从多个线程提交取消请求。这会导致多个线程块被取消，需要谨慎处理，例如： <ul> <li>每个提交线程必须提供各自独立的 <code class="language-plaintext highlighter-rouge">__shared__</code> 结果指针，以避免数据竞争；</li> <li>如果使用同一个屏障进行同步，则必须相应调整到达计数与事务计数。</li> </ul> </li> </ul> <hr/> <h2 id="4122-example-vector-scalar-multiplication示例向量-标量乘法">4.12.2 Example: Vector-Scalar Multiplication（示例：向量-标量乘法）</h2> <blockquote> <p>In the following subsections, we demonstrate work stealing through cluster launch control with a vector-scalar multiplication kernel. We show two variants of the same problem: one using thread blocks and one using thread block clusters.</p> </blockquote> <p>在接下来的小节中，我们用一个<strong>向量-标量乘法（vector-scalar multiplication）</strong>内核来演示通过簇启动控制实现的工作窃取。针对同一问题给出两个变体：一个使用线程块，一个使用线程块簇。</p> <h3 id="41221-use-case-thread-blocks用例线程块">4.12.2.1 Use-case: Thread Blocks（用例：线程块）</h3> <blockquote> <p>The three kernels below demonstrate the <em>Fixed Work per Thread Block</em>, <em>Fixed Number of Thread Blocks</em>, and <em>Cluster Launch Control</em> approaches for vector-scalar multiplication $v := \alpha v$.</p> </blockquote> <p>下面三个内核分别演示了针对向量-标量乘法 $v := \alpha v$ 的<strong>固定每线程块工作量</strong>、<strong>固定线程块数量</strong>与<strong>簇启动控制</strong>三种方法。</p> <ul> <li><strong>Fixed Work per Thread Block（固定每线程块工作量）</strong></li> </ul> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__global__</span> <span class="kt">void</span> <span class="nf">kernel_fixed_work</span><span class="p">(</span><span class="kt">float</span><span class="o">*</span> <span class="n">data</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Prologue:</span>
    <span class="kt">float</span> <span class="n">alpha</span> <span class="o">=</span> <span class="n">compute_scalar</span><span class="p">();</span>
    <span class="c1">// Computation:</span>
    <span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">)</span>
        <span class="n">data</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">*=</span> <span class="n">alpha</span><span class="p">;</span>
<span class="p">}</span>
<span class="c1">// Launch:</span>
<span class="n">kernel_fixed_work</span><span class="o">&lt;&lt;&lt;</span><span class="mi">1024</span><span class="p">,</span> <span class="p">(</span><span class="n">n</span> <span class="o">+</span> <span class="mi">1023</span><span class="p">)</span> <span class="o">/</span> <span class="mi">1024</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
</code></pre></div></div> <ul> <li><strong>Fixed Number of Thread Blocks（固定线程块数量）</strong></li> </ul> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__global__</span> <span class="kt">void</span> <span class="nf">kernel_fixed_blocks</span><span class="p">(</span><span class="kt">float</span><span class="o">*</span> <span class="n">data</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Prologue:</span>
    <span class="kt">float</span> <span class="n">alpha</span> <span class="o">=</span> <span class="n">compute_scalar</span><span class="p">();</span>
    <span class="c1">// Computation:</span>
    <span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">data</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">*=</span> <span class="n">alpha</span><span class="p">;</span>
        <span class="n">i</span> <span class="o">+=</span> <span class="n">gridDim</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
<span class="c1">// Launch:</span>
<span class="n">kernel_fixed_blocks</span><span class="o">&lt;&lt;&lt;</span><span class="mi">1024</span><span class="p">,</span> <span class="n">SM_COUNT</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
</code></pre></div></div> <ul> <li><strong>Cluster Launch Control（簇启动控制）</strong></li> </ul> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;cooperative_groups.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;cuda/ptx&gt;</span><span class="cp">
</span>
<span class="k">namespace</span> <span class="n">cg</span> <span class="o">=</span> <span class="n">cooperative_groups</span><span class="p">;</span>
<span class="k">namespace</span> <span class="n">ptx</span> <span class="o">=</span> <span class="n">cuda</span><span class="o">::</span><span class="n">ptx</span><span class="p">;</span>

<span class="n">__global__</span> <span class="kt">void</span> <span class="nf">kernel_cluster_launch_control</span><span class="p">(</span><span class="kt">float</span><span class="o">*</span> <span class="n">data</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Cluster launch control initialization:</span>
    <span class="n">__shared__</span> <span class="n">uint4</span> <span class="n">result</span><span class="p">;</span>
    <span class="n">__shared__</span> <span class="kt">uint64_t</span> <span class="n">bar</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">phase</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">cg</span><span class="o">::</span><span class="n">thread_block</span><span class="o">::</span><span class="n">thread_rank</span><span class="p">()</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span>
        <span class="n">ptx</span><span class="o">::</span><span class="n">mbarrier_init</span><span class="p">(</span><span class="o">&amp;</span><span class="n">bar</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>

    <span class="c1">// Prologue:</span>
    <span class="kt">float</span> <span class="n">alpha</span> <span class="o">=</span> <span class="n">compute_scalar</span><span class="p">();</span>  <span class="c1">// Device function not shown in this code snippet.</span>

    <span class="c1">// Work-stealing loop:</span>
    <span class="kt">int</span> <span class="n">bx</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>  <span class="c1">// Assuming 1D x-axis thread blocks.</span>
    <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// Protect result from overwrite in the next iteration,</span>
        <span class="c1">// (also ensure barrier initialization at 1st iteration):</span>
        <span class="n">__syncthreads</span><span class="p">();</span>

        <span class="c1">// Cancellation request:</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">cg</span><span class="o">::</span><span class="n">thread_block</span><span class="o">::</span><span class="n">thread_rank</span><span class="p">()</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
            <span class="c1">// Acquire write of result in the async proxy:</span>
            <span class="n">ptx</span><span class="o">::</span><span class="n">fence_proxy_async_generic_sync_restrict</span><span class="p">(</span>
                <span class="n">ptx</span><span class="o">::</span><span class="n">sem_acquire</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">space_cluster</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">scope_cluster</span><span class="p">);</span>
            <span class="n">cg</span><span class="o">::</span><span class="n">invoke_one</span><span class="p">(</span><span class="n">cg</span><span class="o">::</span><span class="n">coalesced_threads</span><span class="p">(),</span> <span class="p">[</span><span class="o">&amp;</span><span class="p">](){</span>
                <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_try_cancel</span><span class="p">(</span><span class="o">&amp;</span><span class="n">result</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bar</span><span class="p">);</span>
            <span class="p">});</span>
            <span class="n">ptx</span><span class="o">::</span><span class="n">mbarrier_arrive_expect_tx</span><span class="p">(</span>
                <span class="n">ptx</span><span class="o">::</span><span class="n">sem_relaxed</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">scope_cta</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">space_shared</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bar</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">uint4</span><span class="p">));</span>
        <span class="p">}</span>

        <span class="c1">// Computation:</span>
        <span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="n">bx</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">)</span>
            <span class="n">data</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">*=</span> <span class="n">alpha</span><span class="p">;</span>

        <span class="c1">// Cancellation request synchronization:</span>
        <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">ptx</span><span class="o">::</span><span class="n">mbarrier_try_wait_parity</span><span class="p">(</span>
            <span class="n">ptx</span><span class="o">::</span><span class="n">sem_acquire</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">scope_cta</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bar</span><span class="p">,</span> <span class="n">phase</span><span class="p">))</span> <span class="p">{}</span>
        <span class="n">phase</span> <span class="o">^=</span> <span class="mi">1</span><span class="p">;</span>

        <span class="c1">// Cancellation request decoding:</span>
        <span class="kt">bool</span> <span class="n">success</span> <span class="o">=</span> <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_query_cancel_is_canceled</span><span class="p">(</span><span class="n">result</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">success</span><span class="p">)</span>
            <span class="k">break</span><span class="p">;</span>
        <span class="n">bx</span> <span class="o">=</span> <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_query_cancel_get_first_ctaid_x</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span><span class="p">(</span><span class="n">result</span><span class="p">);</span>

        <span class="c1">// Release read of result to the async proxy:</span>
        <span class="n">ptx</span><span class="o">::</span><span class="n">fence_proxy_async_generic_sync_restrict</span><span class="p">(</span>
            <span class="n">ptx</span><span class="o">::</span><span class="n">sem_release</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">space_shared</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">scope_cluster</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>
<span class="c1">// Launch:</span>
<span class="n">kernel_cluster_launch_control</span><span class="o">&lt;&lt;&lt;</span><span class="mi">1024</span><span class="p">,</span> <span class="p">(</span><span class="n">n</span> <span class="o">+</span> <span class="mi">1023</span><span class="p">)</span> <span class="o">/</span> <span class="mi">1024</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
</code></pre></div></div> <h3 id="41222-use-case-thread-block-clusters用例线程块簇">4.12.2.2 Use-case: Thread Block Clusters（用例：线程块簇）</h3> <blockquote> <p>In the case of a <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/intro-to-cuda-cpp.html#thread-block-clusters">thread block clusters</a>, the thread block cancellation steps are the same as in a non-cluster setting, with minor adjustments. As in the non-cluster case, submitting a cancellation request from multiple threads <strong>within a cluster</strong> is not recommended, as this will attempt to cancel multiple clusters.</p> </blockquote> <p>在<a href="https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/intro-to-cuda-cpp.html#thread-block-clusters">线程块簇</a>的情况下，线程块取消步骤与非簇环境相同，只有少量调整。与非簇情况一样，<strong>不建议</strong>从簇内的多个线程提交取消请求，因为这会尝试取消多个簇。</p> <blockquote> <ul> <li>The cancellation is submitted by a single cluster thread.</li> </ul> </blockquote> <ul> <li>取消请求由<strong>单个簇线程</strong>提交。</li> </ul> <blockquote> <ul> <li>The shared memory result of each cluster’s thread block will receive the same (encoded) value of the cancelled thread block index (i.e., the result value is multicasted). The result received by all thread blocks corresponds to the local block index <code class="language-plaintext highlighter-rouge">{0, 0, 0}</code> within a cluster. Therefore, thread blocks within the cluster need to add the local block index.</li> </ul> </blockquote> <ul> <li>簇内每个线程块的共享内存结果都会收到<strong>相同</strong>的（编码后的）被取消线程块索引值（即结果值是<strong>多播（multicast）</strong>的）。所有线程块收到的结果对应的是簇内的<strong>本地块索引 <code class="language-plaintext highlighter-rouge">{0, 0, 0}</code></strong>。因此，簇内的线程块需要再加上自己的本地块索引。</li> </ul> <blockquote> <ul> <li>Synchronization is performed by each cluster’s thread block using a local <code class="language-plaintext highlighter-rouge">__shared__</code> memory barrier. Barrier operations must be performed with the <code class="language-plaintext highlighter-rouge">ptx::scope_cluster</code> scope.</li> </ul> </blockquote> <ul> <li>同步由簇内每个线程块使用自己的本地 <code class="language-plaintext highlighter-rouge">__shared__</code> 内存屏障完成。屏障操作必须使用 <code class="language-plaintext highlighter-rouge">ptx::scope_cluster</code> 作用域。</li> </ul> <blockquote> <ul> <li>Cancelling in the cluster case requires all the thread blocks to exist. A user can guarantee that all thread blocks are running by using <code class="language-plaintext highlighter-rouge">cg::cluster_group::sync()</code> from <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/05-appendices/device-callable-apis.html#cg-api-sync-function">sync</a> API.</li> </ul> </blockquote> <ul> <li>在簇的情况下执行取消，要求<strong>所有线程块都已存在</strong>。用户可以通过 <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/05-appendices/device-callable-apis.html#cg-api-sync-function">sync</a> API 中的 <code class="language-plaintext highlighter-rouge">cg::cluster_group::sync()</code> 来确保所有线程块都在运行。</li> </ul> <blockquote> <p>The kernel below demonstrates the cluster launch control approach using thread block clusters.</p> </blockquote> <p>下面的内核演示了使用线程块簇的簇启动控制方法。</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;cooperative_groups.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;cuda/ptx&gt;</span><span class="cp">
</span>
<span class="k">namespace</span> <span class="n">cg</span> <span class="o">=</span> <span class="n">cooperative_groups</span><span class="p">;</span>
<span class="k">namespace</span> <span class="n">ptx</span> <span class="o">=</span> <span class="n">cuda</span><span class="o">::</span><span class="n">ptx</span><span class="p">;</span>

<span class="n">__global__</span> <span class="nf">__cluster_dims__</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span> <span class="kt">void</span> <span class="n">kernel_cluster_launch_control</span><span class="p">(</span><span class="kt">float</span><span class="o">*</span> <span class="n">data</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Cluster launch control initialization:</span>
    <span class="n">__shared__</span> <span class="n">uint4</span> <span class="n">result</span><span class="p">;</span>
    <span class="n">__shared__</span> <span class="kt">uint64_t</span> <span class="n">bar</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">phase</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">cg</span><span class="o">::</span><span class="n">thread_block</span><span class="o">::</span><span class="n">thread_rank</span><span class="p">()</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">ptx</span><span class="o">::</span><span class="n">mbarrier_init</span><span class="p">(</span><span class="o">&amp;</span><span class="n">bar</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>
        <span class="n">ptx</span><span class="o">::</span><span class="n">fence_mbarrier_init</span><span class="p">(</span><span class="n">ptx</span><span class="o">::</span><span class="n">sem_release</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">scope_cluster</span><span class="p">);</span>  <span class="c1">// CGA-level fence.</span>
    <span class="p">}</span>

    <span class="c1">// Prologue:</span>
    <span class="kt">float</span> <span class="n">alpha</span> <span class="o">=</span> <span class="n">compute_scalar</span><span class="p">();</span>  <span class="c1">// Device function not shown in this code snippet.</span>

    <span class="c1">// Work-stealing loop:</span>
    <span class="kt">int</span> <span class="n">bx</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>  <span class="c1">// Assuming 1D x-axis thread blocks.</span>
    <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// Protect result from overwrite in the next iteration,</span>
        <span class="c1">// (also ensure all thread blocks have started at 1st iteration):</span>
        <span class="n">cg</span><span class="o">::</span><span class="n">cluster_group</span><span class="o">::</span><span class="n">sync</span><span class="p">();</span>

        <span class="c1">// Cancellation request by a single cluster thread:</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">cg</span><span class="o">::</span><span class="n">cluster_group</span><span class="o">::</span><span class="n">thread_rank</span><span class="p">()</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
            <span class="c1">// Acquire write of result in the async proxy:</span>
            <span class="n">ptx</span><span class="o">::</span><span class="n">fence_proxy_async_generic_sync_restrict</span><span class="p">(</span>
                <span class="n">ptx</span><span class="o">::</span><span class="n">sem_acquire</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">space_cluster</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">scope_cluster</span><span class="p">);</span>
            <span class="n">cg</span><span class="o">::</span><span class="n">invoke_one</span><span class="p">(</span><span class="n">cg</span><span class="o">::</span><span class="n">coalesced_threads</span><span class="p">(),</span> <span class="p">[</span><span class="o">&amp;</span><span class="p">](){</span>
                <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_try_cancel_multicast</span><span class="p">(</span><span class="o">&amp;</span><span class="n">result</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bar</span><span class="p">);</span>
            <span class="p">});</span>
        <span class="p">}</span>

        <span class="c1">// Cancellation completion tracked by each thread block:</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">cg</span><span class="o">::</span><span class="n">thread_block</span><span class="o">::</span><span class="n">thread_rank</span><span class="p">()</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span>
            <span class="n">ptx</span><span class="o">::</span><span class="n">mbarrier_arrive_expect_tx</span><span class="p">(</span>
                <span class="n">ptx</span><span class="o">::</span><span class="n">sem_relaxed</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">scope_cluster</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">space_shared</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bar</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">uint4</span><span class="p">));</span>

        <span class="c1">// Computation:</span>
        <span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="n">bx</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">)</span>
            <span class="n">data</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">*=</span> <span class="n">alpha</span><span class="p">;</span>

        <span class="c1">// Cancellation request synchronization:</span>
        <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">ptx</span><span class="o">::</span><span class="n">mbarrier_try_wait_parity</span><span class="p">(</span>
            <span class="n">ptx</span><span class="o">::</span><span class="n">sem_acquire</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">scope_cluster</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bar</span><span class="p">,</span> <span class="n">phase</span><span class="p">))</span> <span class="p">{}</span>
        <span class="n">phase</span> <span class="o">^=</span> <span class="mi">1</span><span class="p">;</span>

        <span class="c1">// Cancellation request decoding:</span>
        <span class="kt">bool</span> <span class="n">success</span> <span class="o">=</span> <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_query_cancel_is_canceled</span><span class="p">(</span><span class="n">result</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">success</span><span class="p">)</span>
            <span class="k">break</span><span class="p">;</span>
        <span class="n">bx</span> <span class="o">=</span> <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_query_cancel_get_first_ctaid_x</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span><span class="p">(</span><span class="n">result</span><span class="p">);</span>
        <span class="n">bx</span> <span class="o">+=</span> <span class="n">cg</span><span class="o">::</span><span class="n">cluster_group</span><span class="o">::</span><span class="n">block_index</span><span class="p">().</span><span class="n">x</span><span class="p">;</span>  <span class="c1">// Add local offset.</span>

        <span class="c1">// Release read of result to the async proxy:</span>
        <span class="n">ptx</span><span class="o">::</span><span class="n">fence_proxy_async_generic_sync_restrict</span><span class="p">(</span>
            <span class="n">ptx</span><span class="o">::</span><span class="n">sem_release</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">space_shared</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">scope_cluster</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>
<span class="c1">// Launch:</span>
<span class="n">kernel_cluster_launch_control</span><span class="o">&lt;&lt;&lt;</span><span class="mi">1024</span><span class="p">,</span> <span class="p">(</span><span class="n">n</span> <span class="o">+</span> <span class="mi">1023</span><span class="p">)</span> <span class="o">/</span> <span class="mi">1024</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
</code></pre></div></div> <p><br/></p> <h2 id="原理剖析与实测复现work-stealing-with-cluster-launch-control">原理剖析与实测复现（Work Stealing with Cluster Launch Control）</h2> <p>复现 CUDA Programming Guide 4.12 <em>Work Stealing with Cluster Launch Control</em> （<a href="https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cluster-launch-control.html">https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cluster-launch-control.html</a>）。</p> <p><strong>核心结论</strong>：Work Stealing with Cluster Launch Control 能同时做到两件传统方案做不到的事 —— <strong>大幅减少 block 的预计算（prologue）次数</strong>，<strong>又实现自然的负载均衡</strong>。</p> <hr/> <h3 id="1-背景">1. 背景</h3> <p>CUDA 有两种传统调度做法，各有短板：</p> <table> <thead> <tr> <th>方案</th> <th>减少开销</th> <th>负载均衡</th> </tr> </thead> <tbody> <tr> <td>Fixed Work per Block（每块固定工作量）</td> <td>✗ 每块都要重算 prologue</td> <td>✓（自然的负载均衡指的是是一个block执行结束后，新的blcok会被调度上去）</td> </tr> <tr> <td>Fixed Number of Blocks（固定块数 + loop）</td> <td>✓</td> <td>✗ 静态分配，易长尾</td> </tr> <tr> <td><strong>Cluster Launch Control（工作窃取）</strong></td> <td><strong>✓</strong></td> <td><strong>✓（block执行完后回去窃取从未被调度的block的任务）</strong></td> </tr> </tbody> </table> <p>Cluster Launch Control 是 Blackwell（compute capability 10.0+）特性：线程块可以<strong>异步取消 尚未被调度的其它线程块</strong>，取消成功即「窃取」了对方的工作。</p> <p><strong>环境</strong>：RTX PRO 6000 Blackwell Server Edition（sm_120，188 SM），驱动 580.126.09， CUDA 13.0，<code class="language-plaintext highlighter-rouge">nvcc -arch=sm_120a -O2 -std=c++17</code>。</p> <hr/> <h3 id="2-原理与实验设置">2. 原理与实验设置</h3> <h4 id="21-原理">2.1 原理</h4> <p>每个线程块执行下面这个循环：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>┌──────────────────────────────────────────────┐
│ 1. __syncthreads()   保护上一轮的 result      │
│ 2. 异步发出取消请求 —— 取消一个尚未启动的块   │
│ 3. 计算自己手里的 item   ← 与第 2 步重叠      │
│ 4. 等待取消完成（mbarrier 同步）              │
│ 5. 解码结果：                                 │
│      成功 → 用被取消块的索引替换 bx，回到 1   │
│      失败 → 退出循环                          │
└──────────────────────────────────────────────┘
</code></pre></div></div> <p>第 2、3 步是<strong>异步重叠</strong>的：取消请求发出后不等结果，先去算手里的活，把取消的往返延迟 藏在计算后面。这就是文档说「取消是异步的、用共享内存屏障同步，与异步数据拷贝模式类似」 的含义。</p> <p>三个关键问题：</p> <p><strong>为什么能省 prologue？</strong> prologue 在循环之外，每块只算一次。关键在于<strong>被成功取消的块 从未被调度、从未启动</strong>，所以它那一次 <code class="language-plaintext highlighter-rouge">compute_scalar()</code> 根本没发生。窃取的是「别人的启动 机会」，不是「别人已算出的结果」。因此实际执行 prologue 的块数 = 实际启动过的块数。 实验测到 188，即每个 SM 只启动了一个块，靠反复窃取吃掉了全部 65536 份工作。</p> <p><strong>为什么能自然负载均衡？</strong> 块与 item 之间不再有静态绑定。<code class="language-plaintext highlighter-rouge">bx</code> 是运行时窃取到的索引， 谁先干完谁就去抢下一个未启动的块。慢的块自然少拿、快的块自然多拿，不需要任何调度器干预。 作为对比，<code class="language-plaintext highlighter-rouge">fixed_blocks</code> 的 <code class="language-plaintext highlighter-rouge">item ≡ blockIdx.x (mod 188)</code> 是编译期就定死的静态绑定， 块无法选择自己的工作。</p> <p><strong>循环靠什么终止？</strong> 唯一终止条件是<strong>取消失败</strong>（已经没有未启动的块可抢），此时 <code class="language-plaintext highlighter-rouge">success == false</code>，块退出。注意文档 4.12.1.2 的约束：<strong>观察到一次失败后不能再发出新的取消 请求</strong>（否则是未定义行为）。代码里 <code class="language-plaintext highlighter-rouge">if (!success) break;</code> 立即退出，正好规避了这个约束。</p> <h4 id="22-三种方案的映射">2.2 三种方案的映射</h4> <p>三条 kernel <strong>计算逻辑与数据完全相同</strong>，唯一区别是 work item 如何映射到块：</p> <table> <thead> <tr> <th>方案</th> <th>块数</th> <th>item → 块 的映射</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">fixed_work</code></td> <td>65536</td> <td><code class="language-plaintext highlighter-rouge">item = blockIdx.x</code>，一块一个 item</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">fixed_blocks</code></td> <td>188（= SM 数）</td> <td>grid-stride，<strong>静态绑定</strong>：<code class="language-plaintext highlighter-rouge">item ≡ blockIdx.x (mod 188)</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">cluster_launch</code></td> <td>65536</td> <td><code class="language-plaintext highlighter-rouge">bx</code> 为运行时<strong>窃取</strong>到的索引，<strong>动态</strong></td> </tr> </tbody> </table> <p>为观测效果，代码里补了两个装置（文档未给出 <code class="language-plaintext highlighter-rouge">compute_scalar()</code> 实现）：</p> <ul> <li><strong>真实开销的 prologue</strong>：用串行 FMA 链模拟「卷积系数计算」这类与 blockIdx 无关的昂贵 前置计算。种子取自设备端全局变量防止常量折叠；链的不动点恰为 <code class="language-plaintext highlighter-rouge">2.0f</code>，不影响正确性校验。</li> <li><strong>计数</strong>：每个真正执行了 prologue 的块由 thread 0 原子累加一次，用模板参数开关， 计时时关闭，避免 atomic 污染性能数据。</li> </ul> <p>运行参数：<code class="language-plaintext highlighter-rouge">n = 1&lt;&lt;26 = 67108864</code>（256 MiB，超过 L2），<code class="language-plaintext highlighter-rouge">block = 1024</code>，<code class="language-plaintext highlighter-rouge">grid = 65536</code>。</p> <h4 id="23-完整代码">2.3 完整代码</h4> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Cluster Launch Control — 复现 CUDA Programming Guide 4.12 的「线程块」示例</span>
<span class="c1">// 参考: https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cluster-launch-control.html</span>
<span class="c1">//</span>
<span class="c1">// 特性要求:</span>
<span class="c1">//   - NVIDIA Blackwell 架构 (compute capability 10.0 / sm_100) 及以上（本机 sm_120 满足）</span>
<span class="c1">//   - CUDA 13.0+ / 新版 CCCL (libcu++)，提供 cuda::ptx::clusterlaunchcontrol_* 接口</span>
<span class="c1">//   - 编译目标: -arch=sm_120a（或 sm_100 及以上）</span>
<span class="c1">//</span>
<span class="c1">// 说明: compute_scalar() 在文档里被省略，这里用一个设备端函数模拟</span>
<span class="c1">//       每个线程块都要执行的 "prologue" 开销（例如卷积系数计算）。</span>

<span class="cp">#include</span> <span class="cpf">&lt;cstdio&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;cstdlib&gt;</span><span class="cp">
</span>
<span class="cp">#include</span> <span class="cpf">&lt;cooperative_groups.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;cuda/ptx&gt;</span><span class="cp">
</span>
<span class="k">namespace</span> <span class="n">cg</span> <span class="o">=</span> <span class="n">cooperative_groups</span><span class="p">;</span>
<span class="k">namespace</span> <span class="n">ptx</span> <span class="o">=</span> <span class="n">cuda</span><span class="o">::</span><span class="n">ptx</span><span class="p">;</span>

<span class="c1">// ---------------------------------------------------------------------------</span>
<span class="c1">// 设备端 "prologue"：文档中未给出实现。所有线程块计算得到同一个标量 alpha。</span>
<span class="c1">// 在真实场景里这通常是一段与 blockIdx 无关、但较昂贵的计算（如卷积系数）。</span>
<span class="c1">//</span>
<span class="c1">// 这里用真实开销的串行计算来模拟：</span>
<span class="c1">//   - 从设备端全局变量读取种子 -&gt; 运行时依赖，阻止编译器常量折叠；</span>
<span class="c1">//   - 串行 FMA 依赖链 acc &lt;- 0.5*acc + 1，其不动点恰为 2.0f，</span>
<span class="c1">//     故不同种子最终都收敛到确定的 2.0f，正确性校验不受影响。</span>
<span class="c1">// ---------------------------------------------------------------------------</span>
<span class="k">constexpr</span> <span class="kt">int</span> <span class="n">PROLOGUE_ITERS</span> <span class="o">=</span> <span class="mi">1024</span><span class="p">;</span>   <span class="c1">// prologue 迭代次数，控制开销大小</span>

<span class="n">__device__</span> <span class="kt">float</span> <span class="n">g_prologue_seed</span> <span class="o">=</span> <span class="mf">1.0f</span><span class="p">;</span>

<span class="n">__device__</span> <span class="n">__forceinline__</span> <span class="kt">float</span> <span class="nf">compute_scalar</span><span class="p">()</span> <span class="p">{</span>
    <span class="kt">float</span> <span class="n">acc</span> <span class="o">=</span> <span class="n">g_prologue_seed</span><span class="p">;</span>                 <span class="c1">// 运行时读取，防止常量折叠</span>
<span class="cp">#pragma unroll 1
</span>    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">k</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">k</span> <span class="o">&lt;</span> <span class="n">PROLOGUE_ITERS</span><span class="p">;</span> <span class="o">++</span><span class="n">k</span><span class="p">)</span>
        <span class="n">acc</span> <span class="o">=</span> <span class="n">fmaf</span><span class="p">(</span><span class="n">acc</span><span class="p">,</span> <span class="mf">0.5f</span><span class="p">,</span> <span class="mf">1.0f</span><span class="p">);</span>             <span class="c1">// 不动点 = 2.0f</span>
    <span class="k">return</span> <span class="n">acc</span><span class="p">;</span>
<span class="p">}</span>

<span class="c1">// ---------------------------------------------------------------------------</span>
<span class="c1">// 负载不均（low-tail）设置</span>
<span class="c1">//</span>
<span class="c1">// 每个 work item 附带可变的额外计算量：约 4% 的 item 是"重任务"</span>
<span class="c1">// （计算量是轻任务的 64 倍），且重任务按 item 索引周期分布。</span>
<span class="c1">//</span>
<span class="c1">// 周期取 IMBALANCE_PERIOD = SM 数，这是"固定块数 + grid-stride"的</span>
<span class="c1">// 最坏情形：持久块 b 只会拿到 item ≡ b (mod 188) 的那些任务，</span>
<span class="c1">// 于是恰好落在重任务区间的少数几个块会反复拿到重任务，形成长尾。</span>
<span class="c1">// 而动态（工作窃取）方案不受 item→块 的静态绑定影响。</span>
<span class="c1">// ---------------------------------------------------------------------------</span>
<span class="k">constexpr</span> <span class="kt">int</span> <span class="n">IMBALANCE_PERIOD</span> <span class="o">=</span> <span class="mi">188</span><span class="p">;</span>   <span class="c1">// 取本机 SM 数</span>
<span class="k">constexpr</span> <span class="kt">int</span> <span class="n">HEAVY_ITERS</span> <span class="o">=</span> <span class="mi">1024</span><span class="p">;</span>       <span class="c1">// 重任务额外计算迭代</span>
<span class="k">constexpr</span> <span class="kt">int</span> <span class="n">LIGHT_ITERS</span> <span class="o">=</span> <span class="mi">16</span><span class="p">;</span>         <span class="c1">// 轻任务额外计算迭代</span>

<span class="n">__device__</span> <span class="n">__forceinline__</span> <span class="kt">int</span> <span class="nf">item_extra_iters</span><span class="p">(</span><span class="kt">int</span> <span class="n">item</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">return</span> <span class="p">(</span><span class="n">item</span> <span class="o">%</span> <span class="n">IMBALANCE_PERIOD</span> <span class="o">&lt;</span> <span class="mi">8</span><span class="p">)</span> <span class="o">?</span> <span class="n">HEAVY_ITERS</span> <span class="o">:</span> <span class="n">LIGHT_ITERS</span><span class="p">;</span>
<span class="p">}</span>

<span class="c1">// 模拟单个 work item 自身的可变计算量。串行 FMA 链不动点 = 1.0f，</span>
<span class="c1">// 对 alpha 是乘性单位元，因此不影响最终结果、正确性校验不受影响。</span>
<span class="n">__device__</span> <span class="n">__forceinline__</span> <span class="kt">float</span> <span class="nf">extra_compute</span><span class="p">(</span><span class="kt">int</span> <span class="n">iters</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">float</span> <span class="n">acc</span> <span class="o">=</span> <span class="n">g_prologue_seed</span><span class="p">;</span>
<span class="cp">#pragma unroll 1
</span>    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">k</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">k</span> <span class="o">&lt;</span> <span class="n">iters</span><span class="p">;</span> <span class="o">++</span><span class="n">k</span><span class="p">)</span>
        <span class="n">acc</span> <span class="o">=</span> <span class="n">fmaf</span><span class="p">(</span><span class="n">acc</span><span class="p">,</span> <span class="mf">0.5f</span><span class="p">,</span> <span class="mf">0.5f</span><span class="p">);</span>             <span class="c1">// 不动点 = 1.0f</span>
    <span class="k">return</span> <span class="n">acc</span><span class="p">;</span>
<span class="p">}</span>

<span class="c1">// ---------------------------------------------------------------------------</span>
<span class="c1">// Prologue 执行次数统计</span>
<span class="c1">//</span>
<span class="c1">// 每个真正执行了 compute_scalar() 的线程块记一次（由 thread 0 累加）。</span>
<span class="c1">// 该累加只在模板参数 kCount=true 时编入，因此不会干扰性能测量。</span>
<span class="c1">// ---------------------------------------------------------------------------</span>
<span class="n">__device__</span> <span class="kt">unsigned</span> <span class="kt">long</span> <span class="kt">long</span> <span class="n">g_prologue_count</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

<span class="n">__device__</span> <span class="n">__forceinline__</span> <span class="kt">void</span> <span class="nf">note_prologue</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span>
        <span class="n">atomicAdd</span><span class="p">(</span><span class="o">&amp;</span><span class="n">g_prologue_count</span><span class="p">,</span> <span class="mi">1ULL</span><span class="p">);</span>
<span class="p">}</span>

<span class="c1">// ---------------------------------------------------------------------------</span>
<span class="c1">// 变体 1: Fixed Work per Thread Block（每块固定工作量）</span>
<span class="c1">// 块数 = ceil(n / blockDim)，每块处理固定长度的一段。</span>
<span class="c1">// ---------------------------------------------------------------------------</span>
<span class="k">template</span> <span class="o">&lt;</span><span class="kt">bool</span> <span class="n">kCount</span><span class="p">&gt;</span>
<span class="n">__global__</span> <span class="kt">void</span> <span class="nf">kernel_fixed_work</span><span class="p">(</span><span class="kt">float</span> <span class="o">*</span><span class="n">data</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">float</span> <span class="n">alpha</span> <span class="o">=</span> <span class="n">compute_scalar</span><span class="p">();</span>               <span class="c1">// Prologue</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">kCount</span><span class="p">)</span> <span class="n">note_prologue</span><span class="p">();</span>
    <span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">)</span>
        <span class="c1">// item = blockIdx.x：每块处理一个 item，附带可变计算量</span>
        <span class="n">data</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">*=</span> <span class="n">alpha</span> <span class="o">*</span> <span class="n">extra_compute</span><span class="p">(</span><span class="n">item_extra_iters</span><span class="p">(</span><span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span><span class="p">));</span>
<span class="p">}</span>

<span class="c1">// ---------------------------------------------------------------------------</span>
<span class="c1">// 变体 2: Fixed Number of Thread Blocks（固定块数 + grid-stride）</span>
<span class="c1">// 块数 = SM 数量，每块用 grid-stride 循环遍历全部数据。</span>
<span class="c1">// ---------------------------------------------------------------------------</span>
<span class="k">template</span> <span class="o">&lt;</span><span class="kt">bool</span> <span class="n">kCount</span><span class="p">&gt;</span>
<span class="n">__global__</span> <span class="kt">void</span> <span class="nf">kernel_fixed_blocks</span><span class="p">(</span><span class="kt">float</span> <span class="o">*</span><span class="n">data</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">float</span> <span class="n">alpha</span> <span class="o">=</span> <span class="n">compute_scalar</span><span class="p">();</span>               <span class="c1">// Prologue</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">kCount</span><span class="p">)</span> <span class="n">note_prologue</span><span class="p">();</span>
    <span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// item = i / blockDim.x：静态绑定到本块（stride = gridDim）</span>
        <span class="kt">int</span> <span class="n">item</span> <span class="o">=</span> <span class="n">i</span> <span class="o">/</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
        <span class="n">data</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">*=</span> <span class="n">alpha</span> <span class="o">*</span> <span class="n">extra_compute</span><span class="p">(</span><span class="n">item_extra_iters</span><span class="p">(</span><span class="n">item</span><span class="p">));</span>
        <span class="n">i</span> <span class="o">+=</span> <span class="n">gridDim</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="c1">// ---------------------------------------------------------------------------</span>
<span class="c1">// 变体 3: Cluster Launch Control（线程块工作窃取）</span>
<span class="c1">// ---------------------------------------------------------------------------</span>
<span class="k">template</span> <span class="o">&lt;</span><span class="kt">bool</span> <span class="n">kCount</span><span class="p">&gt;</span>
<span class="n">__global__</span> <span class="kt">void</span> <span class="nf">kernel_cluster_launch_control</span><span class="p">(</span><span class="kt">float</span> <span class="o">*</span><span class="n">data</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Cluster launch control 初始化:</span>
    <span class="n">__shared__</span> <span class="n">uint4</span> <span class="n">result</span><span class="p">;</span>   <span class="c1">// 请求结果</span>
    <span class="n">__shared__</span> <span class="kt">uint64_t</span> <span class="n">bar</span><span class="p">;</span>   <span class="c1">// 同步屏障</span>
    <span class="kt">int</span> <span class="n">phase</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">cg</span><span class="o">::</span><span class="n">thread_block</span><span class="o">::</span><span class="n">thread_rank</span><span class="p">()</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span>
        <span class="n">ptx</span><span class="o">::</span><span class="n">mbarrier_init</span><span class="p">(</span><span class="o">&amp;</span><span class="n">bar</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>

    <span class="kt">float</span> <span class="n">alpha</span> <span class="o">=</span> <span class="n">compute_scalar</span><span class="p">();</span>               <span class="c1">// Prologue</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">kCount</span><span class="p">)</span> <span class="n">note_prologue</span><span class="p">();</span>                  <span class="c1">// 只有真正启动的块会计数</span>

    <span class="c1">// 工作窃取循环:</span>
    <span class="kt">int</span> <span class="n">bx</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>                          <span class="c1">// 假定 1D x 轴线程块</span>
    <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// 防止 result 在下一轮被覆盖（首轮同时保证屏障初始化完成）:</span>
        <span class="n">__syncthreads</span><span class="p">();</span>

        <span class="c1">// 取消请求:</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">cg</span><span class="o">::</span><span class="n">thread_block</span><span class="o">::</span><span class="n">thread_rank</span><span class="p">()</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
            <span class="c1">// 在 async proxy 中获取 result 的写权限:</span>
            <span class="n">ptx</span><span class="o">::</span><span class="n">fence_proxy_async_generic_sync_restrict</span><span class="p">(</span>
                <span class="n">ptx</span><span class="o">::</span><span class="n">sem_acquire</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">space_cluster</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">scope_cluster</span><span class="p">);</span>
            <span class="n">cg</span><span class="o">::</span><span class="n">invoke_one</span><span class="p">(</span><span class="n">cg</span><span class="o">::</span><span class="n">coalesced_threads</span><span class="p">(),</span> <span class="p">[</span><span class="o">&amp;</span><span class="p">]()</span> <span class="p">{</span>
                <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_try_cancel</span><span class="p">(</span><span class="o">&amp;</span><span class="n">result</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bar</span><span class="p">);</span>
            <span class="p">});</span>
            <span class="n">ptx</span><span class="o">::</span><span class="n">mbarrier_arrive_expect_tx</span><span class="p">(</span><span class="n">ptx</span><span class="o">::</span><span class="n">sem_relaxed</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">scope_cta</span><span class="p">,</span>
                                           <span class="n">ptx</span><span class="o">::</span><span class="n">space_shared</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bar</span><span class="p">,</span>
                                           <span class="k">sizeof</span><span class="p">(</span><span class="n">uint4</span><span class="p">));</span>
        <span class="p">}</span>

        <span class="c1">// 计算（含该 item 的可变额外工作量；bx 为当前/窃取到的 item 索引）:</span>
        <span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="n">bx</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">)</span>
            <span class="n">data</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">*=</span> <span class="n">alpha</span> <span class="o">*</span> <span class="nf">extra_compute</span><span class="p">(</span><span class="n">item_extra_iters</span><span class="p">(</span><span class="n">bx</span><span class="p">));</span>

        <span class="c1">// 取消请求同步:</span>
        <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">ptx</span><span class="o">::</span><span class="n">mbarrier_try_wait_parity</span><span class="p">(</span><span class="n">ptx</span><span class="o">::</span><span class="n">sem_acquire</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">scope_cta</span><span class="p">,</span>
                                              <span class="o">&amp;</span><span class="n">bar</span><span class="p">,</span> <span class="n">phase</span><span class="p">))</span> <span class="p">{}</span>

        <span class="n">phase</span> <span class="o">^=</span> <span class="mi">1</span><span class="p">;</span>

        <span class="c1">// 取消请求解码:</span>
        <span class="kt">bool</span> <span class="n">success</span> <span class="o">=</span>
            <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_query_cancel_is_canceled</span><span class="p">(</span><span class="n">result</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">success</span><span class="p">)</span>
            <span class="k">break</span><span class="p">;</span>
        <span class="n">bx</span> <span class="o">=</span> <span class="n">ptx</span><span class="o">::</span><span class="n">clusterlaunchcontrol_query_cancel_get_first_ctaid_x</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span><span class="p">(</span><span class="n">result</span><span class="p">);</span>

        <span class="c1">// 释放 result 的读权限给 async proxy:</span>
        <span class="n">ptx</span><span class="o">::</span><span class="n">fence_proxy_async_generic_sync_restrict</span><span class="p">(</span>
            <span class="n">ptx</span><span class="o">::</span><span class="n">sem_release</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">space_shared</span><span class="p">,</span> <span class="n">ptx</span><span class="o">::</span><span class="n">scope_cluster</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="c1">// ---------------------------------------------------------------------------</span>
<span class="c1">// Host 端驱动与校验</span>
<span class="c1">// ---------------------------------------------------------------------------</span>
<span class="k">static</span> <span class="kt">bool</span> <span class="nf">check</span><span class="p">(</span><span class="k">const</span> <span class="kt">float</span> <span class="o">*</span><span class="n">data</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n</span><span class="p">,</span> <span class="kt">float</span> <span class="n">alpha</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
        <span class="kt">float</span> <span class="n">expect</span> <span class="o">=</span> <span class="p">(</span><span class="kt">float</span><span class="p">)</span><span class="n">i</span> <span class="o">*</span> <span class="n">alpha</span><span class="p">;</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">data</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">!=</span> <span class="n">expect</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">printf</span><span class="p">(</span><span class="s">"Mismatch at %d: got %f, expected %f</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">i</span><span class="p">,</span> <span class="n">data</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="n">expect</span><span class="p">);</span>
            <span class="k">return</span> <span class="nb">false</span><span class="p">;</span>
        <span class="p">}</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="nb">true</span><span class="p">;</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">const</span> <span class="kt">int</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">&lt;&lt;</span> <span class="mi">26</span><span class="p">;</span>      <span class="c1">// 67108864 元素 (256 MB)，grid 远超 SM 数</span>
    <span class="k">const</span> <span class="kt">int</span> <span class="n">block</span> <span class="o">=</span> <span class="mi">1024</span><span class="p">;</span>
    <span class="k">const</span> <span class="kt">int</span> <span class="n">grid</span> <span class="o">=</span> <span class="p">(</span><span class="n">n</span> <span class="o">+</span> <span class="n">block</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span> <span class="o">/</span> <span class="n">block</span><span class="p">;</span>   <span class="c1">// 65536</span>

    <span class="n">cudaDeviceProp</span> <span class="n">prop</span><span class="p">;</span>
    <span class="n">cudaGetDeviceProperties</span><span class="p">(</span><span class="o">&amp;</span><span class="n">prop</span><span class="p">,</span> <span class="mi">0</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"GPU: %s (sm_%d%d)</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">prop</span><span class="p">.</span><span class="n">name</span><span class="p">,</span> <span class="n">prop</span><span class="p">.</span><span class="n">major</span><span class="p">,</span> <span class="n">prop</span><span class="p">.</span><span class="n">minor</span><span class="p">);</span>

    <span class="kt">float</span> <span class="o">*</span><span class="n">d</span><span class="p">;</span>
    <span class="kt">float</span> <span class="o">*</span><span class="n">h</span> <span class="o">=</span> <span class="p">(</span><span class="kt">float</span> <span class="o">*</span><span class="p">)</span><span class="n">malloc</span><span class="p">(</span><span class="n">n</span> <span class="o">*</span> <span class="k">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">));</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">h</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="p">(</span><span class="kt">float</span><span class="p">)</span><span class="n">i</span><span class="p">;</span>
    <span class="n">cudaMalloc</span><span class="p">(</span><span class="o">&amp;</span><span class="n">d</span><span class="p">,</span> <span class="n">n</span> <span class="o">*</span> <span class="k">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">));</span>

    <span class="c1">// 变体 1</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">h</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="p">(</span><span class="kt">float</span><span class="p">)</span><span class="n">i</span><span class="p">;</span>
    <span class="n">cudaMemcpy</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">h</span><span class="p">,</span> <span class="n">n</span> <span class="o">*</span> <span class="k">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">),</span> <span class="n">cudaMemcpyHostToDevice</span><span class="p">);</span>
    <span class="n">kernel_fixed_work</span><span class="o">&lt;</span><span class="nb">false</span><span class="o">&gt;&lt;&lt;&lt;</span><span class="n">grid</span><span class="p">,</span> <span class="n">block</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
    <span class="n">cudaMemcpy</span><span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="n">d</span><span class="p">,</span> <span class="n">n</span> <span class="o">*</span> <span class="k">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">),</span> <span class="n">cudaMemcpyDeviceToHost</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"fixed_work       : %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">check</span><span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="n">n</span><span class="p">,</span> <span class="mf">2.0f</span><span class="p">)</span> <span class="o">?</span> <span class="s">"PASS"</span> <span class="o">:</span> <span class="s">"FAIL"</span><span class="p">);</span>

    <span class="c1">// 变体 2</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">h</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="p">(</span><span class="kt">float</span><span class="p">)</span><span class="n">i</span><span class="p">;</span>
    <span class="n">cudaMemcpy</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">h</span><span class="p">,</span> <span class="n">n</span> <span class="o">*</span> <span class="k">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">),</span> <span class="n">cudaMemcpyHostToDevice</span><span class="p">);</span>
    <span class="n">kernel_fixed_blocks</span><span class="o">&lt;</span><span class="nb">false</span><span class="o">&gt;&lt;&lt;&lt;</span><span class="n">prop</span><span class="p">.</span><span class="n">multiProcessorCount</span><span class="p">,</span> <span class="n">block</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
    <span class="n">cudaMemcpy</span><span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="n">d</span><span class="p">,</span> <span class="n">n</span> <span class="o">*</span> <span class="k">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">),</span> <span class="n">cudaMemcpyDeviceToHost</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"fixed_blocks     : %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">check</span><span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="n">n</span><span class="p">,</span> <span class="mf">2.0f</span><span class="p">)</span> <span class="o">?</span> <span class="s">"PASS"</span> <span class="o">:</span> <span class="s">"FAIL"</span><span class="p">);</span>

    <span class="c1">// 变体 3</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">h</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="p">(</span><span class="kt">float</span><span class="p">)</span><span class="n">i</span><span class="p">;</span>
    <span class="n">cudaMemcpy</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">h</span><span class="p">,</span> <span class="n">n</span> <span class="o">*</span> <span class="k">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">),</span> <span class="n">cudaMemcpyHostToDevice</span><span class="p">);</span>
    <span class="n">kernel_cluster_launch_control</span><span class="o">&lt;</span><span class="nb">false</span><span class="o">&gt;&lt;&lt;&lt;</span><span class="n">grid</span><span class="p">,</span> <span class="n">block</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
    <span class="n">cudaMemcpy</span><span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="n">d</span><span class="p">,</span> <span class="n">n</span> <span class="o">*</span> <span class="k">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">),</span> <span class="n">cudaMemcpyDeviceToHost</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"cluster_launch   : %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">check</span><span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="n">n</span><span class="p">,</span> <span class="mf">2.0f</span><span class="p">)</span> <span class="o">?</span> <span class="s">"PASS"</span> <span class="o">:</span> <span class="s">"FAIL"</span><span class="p">);</span>

    <span class="c1">// ------------------------------------------------------------------</span>
    <span class="c1">// Prologue 执行次数统计（用 &lt;true&gt; 版本单独跑一次，不影响计时）</span>
    <span class="c1">// ------------------------------------------------------------------</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="kt">long</span> <span class="n">zero</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">cnt</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">--- Prologue 执行次数 ---</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>

    <span class="n">cudaMemcpyToSymbol</span><span class="p">(</span><span class="n">g_prologue_count</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">zero</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">zero</span><span class="p">));</span>
    <span class="n">kernel_fixed_work</span><span class="o">&lt;</span><span class="nb">true</span><span class="o">&gt;&lt;&lt;&lt;</span><span class="n">grid</span><span class="p">,</span> <span class="n">block</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
    <span class="n">cudaDeviceSynchronize</span><span class="p">();</span>
    <span class="n">cudaMemcpyFromSymbol</span><span class="p">(</span><span class="o">&amp;</span><span class="n">cnt</span><span class="p">,</span> <span class="n">g_prologue_count</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">cnt</span><span class="p">));</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"fixed_work       : %llu 次</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">cnt</span><span class="p">);</span>

    <span class="n">cudaMemcpyToSymbol</span><span class="p">(</span><span class="n">g_prologue_count</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">zero</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">zero</span><span class="p">));</span>
    <span class="n">kernel_fixed_blocks</span><span class="o">&lt;</span><span class="nb">true</span><span class="o">&gt;&lt;&lt;&lt;</span><span class="n">prop</span><span class="p">.</span><span class="n">multiProcessorCount</span><span class="p">,</span> <span class="n">block</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
    <span class="n">cudaDeviceSynchronize</span><span class="p">();</span>
    <span class="n">cudaMemcpyFromSymbol</span><span class="p">(</span><span class="o">&amp;</span><span class="n">cnt</span><span class="p">,</span> <span class="n">g_prologue_count</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">cnt</span><span class="p">));</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"fixed_blocks     : %llu 次</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">cnt</span><span class="p">);</span>

    <span class="n">cudaMemcpyToSymbol</span><span class="p">(</span><span class="n">g_prologue_count</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">zero</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">zero</span><span class="p">));</span>
    <span class="n">kernel_cluster_launch_control</span><span class="o">&lt;</span><span class="nb">true</span><span class="o">&gt;&lt;&lt;&lt;</span><span class="n">grid</span><span class="p">,</span> <span class="n">block</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
    <span class="n">cudaDeviceSynchronize</span><span class="p">();</span>
    <span class="n">cudaMemcpyFromSymbol</span><span class="p">(</span><span class="o">&amp;</span><span class="n">cnt</span><span class="p">,</span> <span class="n">g_prologue_count</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">cnt</span><span class="p">));</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"cluster_launch   : %llu 次</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">cnt</span><span class="p">);</span>

    <span class="c1">// ------------------------------------------------------------------</span>
    <span class="c1">// 性能测量：CUDA event 计时，多次迭代取平均。</span>
    <span class="c1">// 注：重复执行会反复乘 alpha 导致数值溢出，但不影响计时；</span>
    <span class="c1">//     正确性已在上面的单次运行中校验过。</span>
    <span class="c1">// ------------------------------------------------------------------</span>
    <span class="k">const</span> <span class="kt">int</span> <span class="n">iters</span> <span class="o">=</span> <span class="mi">20</span><span class="p">;</span>
    <span class="n">cudaEvent_t</span> <span class="n">t0</span><span class="p">,</span> <span class="n">t1</span><span class="p">;</span>
    <span class="n">cudaEventCreate</span><span class="p">(</span><span class="o">&amp;</span><span class="n">t0</span><span class="p">);</span>
    <span class="n">cudaEventCreate</span><span class="p">(</span><span class="o">&amp;</span><span class="n">t1</span><span class="p">);</span>
    <span class="kt">float</span> <span class="n">ms</span> <span class="o">=</span> <span class="mf">0.f</span><span class="p">;</span>

    <span class="c1">// 计时：Fixed Work per Thread Block</span>
    <span class="n">kernel_fixed_work</span><span class="o">&lt;</span><span class="nb">false</span><span class="o">&gt;&lt;&lt;&lt;</span><span class="n">grid</span><span class="p">,</span> <span class="n">block</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>          <span class="c1">// warmup</span>
    <span class="n">cudaDeviceSynchronize</span><span class="p">();</span>
    <span class="n">cudaEventRecord</span><span class="p">(</span><span class="n">t0</span><span class="p">);</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">it</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">it</span> <span class="o">&lt;</span> <span class="n">iters</span><span class="p">;</span> <span class="o">++</span><span class="n">it</span><span class="p">)</span> <span class="n">kernel_fixed_work</span><span class="o">&lt;</span><span class="nb">false</span><span class="o">&gt;&lt;&lt;&lt;</span><span class="n">grid</span><span class="p">,</span> <span class="n">block</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
    <span class="n">cudaEventRecord</span><span class="p">(</span><span class="n">t1</span><span class="p">);</span>
    <span class="n">cudaEventSynchronize</span><span class="p">(</span><span class="n">t1</span><span class="p">);</span>
    <span class="n">cudaEventElapsedTime</span><span class="p">(</span><span class="o">&amp;</span><span class="n">ms</span><span class="p">,</span> <span class="n">t0</span><span class="p">,</span> <span class="n">t1</span><span class="p">);</span>
    <span class="k">const</span> <span class="kt">float</span> <span class="n">t_fixed_work</span> <span class="o">=</span> <span class="n">ms</span> <span class="o">/</span> <span class="n">iters</span><span class="p">;</span>

    <span class="c1">// 计时：Fixed Number of Thread Blocks</span>
    <span class="n">kernel_fixed_blocks</span><span class="o">&lt;</span><span class="nb">false</span><span class="o">&gt;&lt;&lt;&lt;</span><span class="n">prop</span><span class="p">.</span><span class="n">multiProcessorCount</span><span class="p">,</span> <span class="n">block</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>   <span class="c1">// warmup</span>
    <span class="n">cudaDeviceSynchronize</span><span class="p">();</span>
    <span class="n">cudaEventRecord</span><span class="p">(</span><span class="n">t0</span><span class="p">);</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">it</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">it</span> <span class="o">&lt;</span> <span class="n">iters</span><span class="p">;</span> <span class="o">++</span><span class="n">it</span><span class="p">)</span>
        <span class="n">kernel_fixed_blocks</span><span class="o">&lt;</span><span class="nb">false</span><span class="o">&gt;&lt;&lt;&lt;</span><span class="n">prop</span><span class="p">.</span><span class="n">multiProcessorCount</span><span class="p">,</span> <span class="n">block</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
    <span class="n">cudaEventRecord</span><span class="p">(</span><span class="n">t1</span><span class="p">);</span>
    <span class="n">cudaEventSynchronize</span><span class="p">(</span><span class="n">t1</span><span class="p">);</span>
    <span class="n">cudaEventElapsedTime</span><span class="p">(</span><span class="o">&amp;</span><span class="n">ms</span><span class="p">,</span> <span class="n">t0</span><span class="p">,</span> <span class="n">t1</span><span class="p">);</span>
    <span class="k">const</span> <span class="kt">float</span> <span class="n">t_fixed_blocks</span> <span class="o">=</span> <span class="n">ms</span> <span class="o">/</span> <span class="n">iters</span><span class="p">;</span>

    <span class="c1">// 计时：Cluster Launch Control</span>
    <span class="n">kernel_cluster_launch_control</span><span class="o">&lt;</span><span class="nb">false</span><span class="o">&gt;&lt;&lt;&lt;</span><span class="n">grid</span><span class="p">,</span> <span class="n">block</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>             <span class="c1">// warmup</span>
    <span class="n">cudaDeviceSynchronize</span><span class="p">();</span>
    <span class="n">cudaEventRecord</span><span class="p">(</span><span class="n">t0</span><span class="p">);</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">it</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">it</span> <span class="o">&lt;</span> <span class="n">iters</span><span class="p">;</span> <span class="o">++</span><span class="n">it</span><span class="p">)</span>
        <span class="n">kernel_cluster_launch_control</span><span class="o">&lt;</span><span class="nb">false</span><span class="o">&gt;&lt;&lt;&lt;</span><span class="n">grid</span><span class="p">,</span> <span class="n">block</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
    <span class="n">cudaEventRecord</span><span class="p">(</span><span class="n">t1</span><span class="p">);</span>
    <span class="n">cudaEventSynchronize</span><span class="p">(</span><span class="n">t1</span><span class="p">);</span>
    <span class="n">cudaEventElapsedTime</span><span class="p">(</span><span class="o">&amp;</span><span class="n">ms</span><span class="p">,</span> <span class="n">t0</span><span class="p">,</span> <span class="n">t1</span><span class="p">);</span>
    <span class="k">const</span> <span class="kt">float</span> <span class="n">t_clc</span> <span class="o">=</span> <span class="n">ms</span> <span class="o">/</span> <span class="n">iters</span><span class="p">;</span>

    <span class="n">printf</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">--- 性能 (n=%d 元素, grid=%d, SM=%d, %d 次取平均) ---</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">n</span><span class="p">,</span> <span class="n">grid</span><span class="p">,</span>
           <span class="n">prop</span><span class="p">.</span><span class="n">multiProcessorCount</span><span class="p">,</span> <span class="n">iters</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"fixed_work       : %8.4f ms</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">t_fixed_work</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"fixed_blocks     : %8.4f ms  (grid=%d)</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">t_fixed_blocks</span><span class="p">,</span>
           <span class="n">prop</span><span class="p">.</span><span class="n">multiProcessorCount</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"cluster_launch   : %8.4f ms</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">t_clc</span><span class="p">);</span>

    <span class="n">cudaFree</span><span class="p">(</span><span class="n">d</span><span class="p">);</span>
    <span class="n">free</span><span class="p">(</span><span class="n">h</span><span class="p">);</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <hr/> <h3 id="3-结果">3. 结果</h3> <h4 id="31-减少-prologue-次数">3.1 减少 prologue 次数</h4> <table> <thead> <tr> <th>变体</th> <th>启动块数</th> <th><strong>Prologue 执行次数</strong></th> </tr> </thead> <tbody> <tr> <td>fixed_work</td> <td>65536</td> <td><strong>65536</strong></td> </tr> <tr> <td>fixed_blocks</td> <td>188</td> <td><strong>188</strong></td> </tr> <tr> <td><strong>cluster_launch</strong></td> <td>65536</td> <td><strong>188</strong></td> </tr> </tbody> </table> <p><code class="language-plaintext highlighter-rouge">cluster_launch</code> 启动了 65536 个块，但<strong>只有 188 个真正跑起来</strong>——其余 65348 个在被调度前 就被成功取消，<strong>从未执行 prologue</strong>。188 恰等于 SM 数，即每个 SM 只启动一个常驻块， 靠工作窃取吃掉全部工作。</p> <p><strong>Prologue 次数降低 348.6 倍</strong>（65536 ÷ 188）。这与 <code class="language-plaintext highlighter-rouge">fixed_blocks</code> 的表现完全相同， 因为它同样只跑 188 个块。</p> <h4 id="32-负载均衡">3.2 负载均衡</h4> <p>给每个 item 附加可变的计算量：约 4% 的 item 是「重任务」（计算量 64 倍），且重任务按 item 索引周期分布，周期取 SM 数（188）。</p> <p>这个周期是刻意选的，因为对 <code class="language-plaintext highlighter-rouge">fixed_blocks</code> 的静态绑定最致命。grid-stride 循环中</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>item = i / blockDim.x = blockIdx.x + k · 188
</code></pre></div></div> <p>故 <code class="language-plaintext highlighter-rouge">item % 188 == blockIdx.x</code>，与迭代序号 k 无关 —— <strong>持久块 b 一辈子只会拿到 item ≡ b (mod 188) 的任务</strong>。于是块 0~7 每个 item 都是重任务，各背约 349 个； 块 8~187 一个重任务都拿不到。</p> <p>实测结果（20 次平均）：</p> <table> <thead> <tr> <th>变体</th> <th>耗时</th> <th>相对最快</th> </tr> </thead> <tbody> <tr> <td>fixed_work</td> <td>6.4289 ms</td> <td>8.8×</td> </tr> <tr> <td><strong>fixed_blocks</strong></td> <td><strong>6.7512 ms</strong></td> <td><strong>9.3×（最差）</strong></td> </tr> <tr> <td><strong>cluster_launch</strong></td> <td><strong>0.7261 ms</strong></td> <td><strong>1.00×（最快）</strong></td> </tr> </tbody> </table> <p><code class="language-plaintext highlighter-rouge">fixed_blocks</code> 从「优等生」变成全场最差：块 0~7 独吞重任务需 <code class="language-plaintext highlighter-rouge">349 × 18.4 µs ≈ 6.4 ms</code>， 这期间其余 180 个 SM 全部空闲——<strong>这就是长尾</strong>。而 <code class="language-plaintext highlighter-rouge">cluster_launch</code> 不受静态绑定约束， 总工作量被 188 个 SM 均摊，只需 0.73 ms。</p> <h4 id="33-对照负载均匀时看不出差别">3.3 对照：负载均匀时看不出差别</h4> <p>把负载改成完全均匀（去掉 3.2 节的额外计算），结果变成：</p> <table> <thead> <tr> <th>变体</th> <th>耗时</th> </tr> </thead> <tbody> <tr> <td>fixed_work</td> <td>6.4529 ms</td> </tr> <tr> <td>fixed_blocks</td> <td>0.3945 ms</td> </tr> <tr> <td>cluster_launch</td> <td>0.4058 ms</td> </tr> </tbody> </table> <p>此时 <code class="language-plaintext highlighter-rouge">cluster_launch</code> 反而比 <code class="language-plaintext highlighter-rouge">fixed_blocks</code> 慢 3%——因为每个块要窃取约 349 次， 每次窃取都有固定的 cancel + mbarrier 同步成本。<strong>没有负载不均，工作窃取就只是白付开销。</strong></p> <hr/> <h3 id="4-结论">4. 结论</h3> <ol> <li><strong>减少 prologue</strong>：<code class="language-plaintext highlighter-rouge">cluster_launch</code> 只执行 188 次 prologue，与「固定块数」方案持平， 比「每块固定工作量」的 65536 次<strong>少 348.6 倍</strong>。被取消的块根本不启动，也就不会执行预计算。</li> <li><strong>自然的负载均衡</strong>：负载不均时，<code class="language-plaintext highlighter-rouge">fixed_blocks</code> 因静态绑定被长尾拖到 6.75 ms， 而 <code class="language-plaintext highlighter-rouge">cluster_launch</code> 靠动态窃取把工作摊平，只要 0.73 ms，<strong>快 9.3 倍</strong>。</li> <li><strong>两者兼得</strong>：<code class="language-plaintext highlighter-rouge">cluster_launch</code> 同时具备「固定块数」的低 prologue 开销和 「每块固定工作量」的动态均衡能力。文档表格中的三项结论全部实测复现。</li> <li><strong>收益有前提</strong>：必须存在随规模增长的冗余 prologue 开销，且负载不均或存在 low-tail。 负载均匀时三者差异消失，甚至因窃取成本略慢。</li> </ol> <hr/> <h3 id="5-复现">5. 复现</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nvcc <span class="nt">-arch</span><span class="o">=</span>sm_120a <span class="nt">-O2</span> <span class="nt">-std</span><span class="o">=</span>c++17 cluster_launch_control.cu <span class="nt">-o</span> clc <span class="o">&amp;&amp;</span> ./clc
</code></pre></div></div> <p>预期输出：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>fixed_work       : PASS
fixed_blocks     : PASS
cluster_launch   : PASS

--- Prologue 执行次数 ---
fixed_work       : 65536 次
fixed_blocks     : 188 次
cluster_launch   : 188 次

--- 性能 (n=67108864 元素, grid=65536, SM=188, 20 次取平均) ---
fixed_work       :   6.4332 ms
fixed_blocks     :   6.7517 ms  (grid=188)
cluster_launch   :   0.7278 ms
</code></pre></div></div> <hr/> <h3 id="6-注意事项">6. 注意事项</h3> <ol> <li><strong>重任务周期取 188 是刻意构造的最坏情形</strong>，目的是把「静态绑定」的弱点放大到肉眼可见。 真实负载的长尾通常来自数据自身分布，不会如此规整，但失效机理相同。</li> <li><strong><code class="language-plaintext highlighter-rouge">compute_scalar()</code></strong> <strong>与额外计算均为模拟</strong>。属合成负载（microbenchmark）， 结论适用于机制验证，不能直接外推为具体应用的加速比。</li> <li><strong>单次 prologue 开销 ≈ 18.4 µs</strong> 由实验数据反推，非直接测量。</li> <li>本机 4 张 GPU 中有 1 张出现 GSP 通信失败（需重启恢复），实验全部在 GPU 0 上完成，不受影响。</li> </ol>]]></content><author><name></name></author><category term="cuda"/><summary type="html"><![CDATA[《CUDA C++ Programming Guide》13.3 §4.12 Work Stealing with Cluster Launch Control 的中英对照翻译，附工作窃取原理剖析与可直接编译运行的完整代码（Blackwell sm_120 实测）。]]></summary></entry><entry><title type="html">我所理解的 CUDA · 第 1 章：Programming Model</title><link href="https://chriszhao13.github.io/blog/2026/programming-model/" rel="alternate" type="text/html" title="我所理解的 CUDA · 第 1 章：Programming Model"/><published>2026-08-30T02:00:00+00:00</published><updated>2026-08-30T02:00:00+00:00</updated><id>https://chriszhao13.github.io/blog/2026/programming-model</id><content type="html" xml:base="https://chriszhao13.github.io/blog/2026/programming-model/"><![CDATA[<blockquote> <p>本节对应《CUDA C++ Programming Guide》Release 13.3 <strong>第 1 章 Programming Model，§1.2.1 Heterogeneous Systems</strong>，采用中英对照。</p> </blockquote> <blockquote> <p>This chapter introduces the CUDA programming model at a high level and separate from any language. The terminology and concepts introduced here apply to CUDA in any supported programming language. Later chapters will illustrate these concepts in C++.</p> </blockquote> <p>本章从较高层次介绍 CUDA 编程模型，并且不依赖于任何具体语言。本章引入的术语和概念适用于任何受支持的编程语言中的 CUDA。后续章节将用 C++ 来具体阐述这些概念。</p> <h2 id="121-heterogeneous-systems异构系统">1.2.1 Heterogeneous Systems（异构系统）</h2> <blockquote> <p>The CUDA programming model assumes a heterogeneous computing system, which means a system that includes both GPUs and CPUs. The CPU and the memory directly connected to it are called the host and host memory, respectively. A GPU and the memory directly connected to it are referred to as the device and device memory, respectively. In some system-on-chip (SoC) systems, these may be part of a single package. In larger systems, there may be multiple CPUs or GPUs.</p> </blockquote> <p>CUDA 编程模型假设系统为异构计算系统，即同时包含 GPU 和 CPU 的系统。CPU 及其直接相连的内存分别称为<strong>主机（host）</strong>和<strong>主机内存（host memory）</strong>；GPU 及其直接相连的内存分别称为<strong>设备（device）</strong>和<strong>设备内存（device memory）</strong>。在某些片上系统（SoC）中，这些组件可能集成于同一封装之内。在规模更大的系统中，则可能包含多个 CPU 或多个 GPU</p> <blockquote> <p>CUDA applications execute some part of their code on the GPU, but applications always start execution on the CPU. The host code, which is the code that runs on the CPU, can use CUDA APIs to copy data between the host memory and device memory, start code executing on the GPU, and wait for data copies or GPU code to complete. The CPU and GPU can both be executing code simultaneously, and best performance is usually found by maximizing utilization of both CPUs and GPUs.</p> </blockquote> <p>CUDA 应用程序将部分代码在 GPU 上执行，但程序总是从 CPU 开始运行。主机代码（即运行在 CPU 上的代码）可通过 CUDA API 在主机内存与设备内存之间复制数据、启动 GPU 上的代码执行，并等待数据拷贝或 GPU 代码完成。CPU 与 GPU 可以同时执行各自的代码，而最佳性能通常通过最大化 CPU 与 GPU 的利用率来获得。</p> <blockquote> <p>The code an application executes on the GPU is referred to as device code, and a function that is invoked for execution on the GPU is, for historical reasons, called a kernel. The act of starting a kernel running is called launching the kernel. A kernel launch can be thought of as starting many threads executing the kernel code in parallel on the GPU. GPU threads operate similarly to threads on CPUs, though there are some differences important to both correctness and performance that will be covered in later sections (see Section 3.2.2.1.1).</p> </blockquote> <p>应用程序在 GPU 上执行的代码称为<strong>设备代码（device code）</strong>；出于历史原因，被调用到 GPU 上执行的函数被称为<strong>内核（kernel）</strong>。启动一个内核运行的行为称为<strong>启动内核（launching the kernel）</strong>。可以把内核启动理解为：让大量线程在 GPU 上并行执行该内核代码。GPU 线程的运作方式与 CPU 线程类似，不过二者存在一些对正确性和性能都很重要的差异，这些差异将在后续章节中介绍（参见 <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/03-advanced/advanced-kernel-programming.html#advanced-kernels-independent-thread-scheduling">https://docs.nvidia.com/cuda/cuda-programming-guide/03-advanced/advanced-kernel-programming.html#advanced-kernels-independent-thread-scheduling</a>）。(注：什么叫正确性的差异？)</p> <hr/> <h2 id="122-gpu-hardware-modelgpu-硬件模型">1.2.2 GPU Hardware Model（GPU 硬件模型）</h2> <blockquote> <p>Like any programming model, CUDA relies on a conceptual model of the underlying hardware. For the purposes of CUDA programming, the GPU can be considered to be a collection of Streaming Multiprocessors (SMs) which are organized into groups called Graphics Processing Clusters (GPCs). Each SM contains a local register file, a unified data cache, and a number of functional units that perform computations. The unified data cache provides the physical resources for shared memory and L1 cache. The allocation of the unified data cache to L1 and shared memory can be configured at runtime. The sizes of different types of memory and the number of functional units within an SM can vary across GPU architectures.</p> </blockquote> <p>与任何编程模型一样，CUDA 也建立在底层硬件的一个概念模型之上（CUDA 的抽象是对硬件的一种简化视角，方便程序员不关心具体微架构细节也能编程）。就 CUDA 编程而言，可以把 GPU 看作一组<strong>流式多处理器（Streaming Multiprocessors，简称 SM）</strong>的集合，这些 SM 被组织成称为<strong>图形处理簇（Graphics Processing Clusters，简称 GPC）</strong>的分组。每个 SM 包含一个本地寄存器文件、一个统一数据缓存，以及若干执行计算的功能单元。统一数据缓存为共享内存和 L1 缓存提供物理资源。<strong>统一数据缓存在 L1 与共享内存之间的分配比例可以在运行时配置</strong>。不同类型内存的容量以及 SM 内功能单元的数量会因 GPU 架构而异。</p> <blockquote> <p>Note: The actual hardware layout of a GPU or the way it physically carries out the execution of the programming model may vary. These differences do not affect correctness of software written using the CUDA programming model.</p> </blockquote> <p><strong>注</strong>：GPU 的实际硬件布局，或它在物理层面执行该编程模型的具体方式，可能会有所不同。但这些差异不影响使用 CUDA 编程模型编写的软件的正确性。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/cuda101/image02-480.webp 480w,/assets/img/cuda101/image02-800.webp 800w,/assets/img/cuda101/image02-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/cuda101/image02.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <div class="caption"> 图 2：GPU 包含许多流式多处理器（SM），每个 SM 内部又包含大量功能单元。图形处理簇（GPC）是 SM 的集合。一个 GPU 由一组连接到 GPU 内存的 GPC 组成。CPU 通常具有多个核心，以及一个连接到系统内存的内存控制器。CPU 与 GPU 之间通过诸如 PCIe 或 NVLink 之类的互连进行连接。 </div> <hr/> <h3 id="1221-thread-blocks-and-grids">1.2.2.1. Thread Blocks and Grids</h3> <blockquote> <p>When an application launches a kernel, it does so with many threads, often millions of threads. These threads are organized into blocks. A block of threads is referred to, perhaps unsurprisingly, as a thread block. Thread blocks are organized into a grid. All the thread blocks in a grid have the same size and dimensions. Figure 3 shows an illustration of a grid of thread blocks.</p> </blockquote> <p>当应用程序启动一个内核时，会同时启动大量线程，通常多达数百万个。这些线程被组织成<strong>块（block）</strong>。一个线程块被称为<strong>线程块（thread block）</strong>—— 这个名字或许并不令人意外。线程块进一步被组织成<strong>网格（grid）</strong>。一个网格中的所有线程块具有相同的大小和维度。图 3 展示了一个由线程块组成的网格示意图。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/cuda101/image03-480.webp 480w,/assets/img/cuda101/image03-800.webp 800w,/assets/img/cuda101/image03-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/cuda101/image03.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <div class="caption"> 图 3：线程块网格。每个箭头代表一个线程（箭头数量并不代表实际线程数量）。 </div> <blockquote> <p>Thread blocks and grids may be 1, 2, or 3 dimensional. These dimensions can simplify mapping of individual threads to units of work or data items.</p> </blockquote> <p>线程块和网格可以是一维、二维或三维的。这些维度可以简化将单个线程映射到工作单元或数据项的过程。</p> <blockquote> <p>When a kernel is launched, it is launched using a specific execution configuration which specifies the grid and thread block dimensions. The execution configuration may also include optional parameters such as cluster size, stream, and SM configuration settings, which will be introduced in later sections.</p> </blockquote> <p>启动内核时，需要使用一个特定的<strong>执行配置（execution configuration）</strong>，其中指定了网格和线程块的维度。执行配置还可以包含一些可选参数，例如<strong>簇大小（cluster size）</strong>、<strong>流（stream）</strong>以及 <strong>SM 配置设置</strong>等，这些内容将在后续章节中介绍。</p> <p>注：<strong>“cluster size”</strong> → “簇大小”，指 thread block cluster（线程块簇），这是 Hopper 架构引入的新特性，允许多个 block 组成一个 cluster 协同执行，共享分布式共享内存。属于较新的进阶内容。</p> <blockquote> <p>Using built-in variables, each thread executing the kernel can determine its location within its containing block and the location of its block within the containing grid. A thread can also use these built-in variables to determine the dimensions of the thread blocks and the grid on which the kernel was launched. This gives each thread a unique identity among all the threads running the kernel. This identity is frequently used to determine what data or operations a thread is responsible for.</p> </blockquote> <p>通过使用内置变量，执行内核的每个线程都可以确定自己在所属线程块中的位置，以及其所属线程块在所属网格中的位置。线程还可以利用这些内置变量确定内核启动时所使用的线程块和网格的维度。这使得每个线程在所有运行该内核的线程中都拥有一个唯一标识。该标识通常用于确定一个线程负责处理哪些数据或执行哪些操作。</p> <blockquote> <p>All threads of a thread block are executed in a single SM. This allows threads within a thread block to communicate and synchronize with each other efficiently. Threads within a thread block all have access to the on-chip shared memory, which can be used for exchanging information between threads of a thread block.</p> </blockquote> <p>一个线程块中的所有线程都在<strong>同一个 SM 上执行</strong>。这使得线程块内的线程能够高效地相互通信和同步。线程块内的所有线程都可以访问<strong>片上共享内存（on-chip shared memory）</strong>，该内存可用于线程块内线程之间交换信息。</p> <blockquote> <p>A grid may consist of millions of thread blocks, while the GPU executing the grid may have only tens or hundreds of SMs. All threads of a thread block are executed by a single SM and, in most cases [1], run to completion on that SM. There is no guarantee of scheduling between thread blocks, so a thread block cannot rely on results from other thread blocks, as they may not be able to be scheduled until that thread block has completed. Figure 4 shows an example of how thread blocks from a grid are assigned to an SM.</p> </blockquote> <p>一个网格可能包含数百万个线程块，而执行该网格的 GPU 可能只有几十个或上百个 SM。一个线程块的所有线程都由同一个 SM 执行，并且在大多数情况下 [1] 会在该 SM 上运行至结束。线程块之间的调度没有任何保证，因此一个线程块不能依赖其他线程块的结果 —— 因为那些线程块可能要等到当前线程块完成后才能被调度。图 4 展示了网格中的线程块如何被分配到 SM 上的一个示例。</p> <p>[1] In certain situations when using features such as <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/dynamic-parallelism.html#cuda-dynamic-parallelism">https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/dynamic-parallelism.html#cuda-dynamic-parallelism</a> , a thread block may be suspended to memory. This means the state of the SM is stored to a system-managed area of GPU memory and the SM is freed to execute other thread blocks. This is similar to context swapping on CPUs. This is not common.</p> <p>[1] 在某些情况下，当使用诸如<strong>动态并行（dynamic parallelism）</strong>（<a href="https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/dynamic-parallelism.html#cuda-dynamic-parallelism">https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/dynamic-parallelism.html#cuda-dynamic-parallelism</a>）等特性时，一个线程块可能会被<strong>挂起到内存（suspended to memory）</strong>。这意味着该 SM 的状态被保存到 GPU 内存中由系统管理的区域，同时该 SM 被释放出来以执行其他线程块。这类似于 CPU 上的<strong>上下文切换（context swapping）</strong>。这种情况并不常见。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/cuda101/image04-480.webp 480w,/assets/img/cuda101/image04-800.webp 800w,/assets/img/cuda101/image04-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/cuda101/image04.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <div class="caption"> 图 4：每个 SM 有一个或多个活跃的线程块。在本示例中，每个 SM 同时调度了三个线程块。网格中的线程块以何种顺序分配到各个 SM，没有任何保证。 </div> <blockquote> <p>The CUDA programming model enables arbitrarily large grids to run on GPUs of any size, whether it has only one SM or thousands of SMs. To achieve this, the CUDA programming model, with some exceptions, requires that there be no data dependencies between threads in different thread blocks. That is, a thread should not depend on results from or synchronize with a thread in a different thread block of the same grid. All the threads within a thread block run on the same SM at the same time. Different thread blocks within the grid are scheduled among the available SMs and may be executed in any order. In short, the CUDA programming model requires that it be possible to execute thread blocks in any order, in parallel or in series.</p> </blockquote> <p>CUDA 编程模型使得任意大小的网格都能在任何规模的 GPU 上运行 —— 无论该 GPU 只有一个 SM，还是拥有数千个 SM。为了实现这一点，CUDA 编程模型（除少数例外情况外）要求<strong>不同线程块中的线程之间不存在数据依赖</strong>。也就是说，一个线程不应依赖同一网格中另一个线程块内线程的结果，也不应与之同步。一个线程块内的所有线程同时在同一个 SM 上运行。网格中的不同线程块被调度到可用的 SM 上，并且可以以任意顺序执行。简而言之，CUDA 编程模型要求：线程块必须能够以任意顺序执行，无论是并行执行还是串行执行。</p> <h3 id="12211-thread-block-clusters">1.2.2.1.1. Thread Block Clusters</h3> <blockquote> <p>In addition to thread blocks, GPUs with compute capability 9.0 and higher have an optional level of grouping called clusters. Clusters are a group of thread blocks which, like thread blocks and grids, can be laid out in 1, 2, or 3 dimensions. <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/01-introduction/programming-model.html#figure-thread-block-clusters">https://docs.nvidia.com/cuda/cuda-programming-guide/01-introduction/programming-model.html#figure-thread-block-clusters</a> illustrates a grid of thread blocks that is also organized into clusters. Specifying clusters does not change the grid dimensions or the indices of a thread block within a grid.</p> </blockquote> <p>除线程块之外，计算能力（compute capability）为 9.0 及更高版本的 GPU 还提供了一个可选的分组层级，称为<strong>簇（cluster）</strong>。簇是一组线程块的集合，与线程块和网格一样，也可以按一维、二维或三维布局。<a href="https://docs.nvidia.com/cuda/cuda-programming-guide/01-introduction/programming-model.html#figure-thread-block-clusters">https://docs.nvidia.com/cuda/cuda-programming-guide/01-introduction/programming-model.html#figure-thread-block-clusters</a> 展示了一个同时被组织为簇的线程块网格示意图。指定簇并不会改变网格的维度，也不会改变线程块在网格中的索引。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/cuda101/image05-480.webp 480w,/assets/img/cuda101/image05-800.webp 800w,/assets/img/cuda101/image05-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/cuda101/image05.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <div class="caption"> 图 5：当指定了簇时，线程块在网格中仍处于原来的位置，但同时也拥有其在所属簇中的位置。 </div> <blockquote> <p>Specifying clusters groups adjacent thread blocks into clusters and provides some additional opportunities for synchronization and communication at the cluster level. Specifically, all thread blocks in a cluster are executed in a single GPC. Figure 6 shows how thread blocks are scheduled to SMs in a GPC when clusters are specified. Because the thread blocks are scheduled simultaneously and within a single GPC, threads in different blocks but within the same cluster can communicate and synchronize with each other using software interfaces provided by Cooperative Groups. Threads in clusters can access the shared memory of all blocks in the cluster, which is referred to as distributed shared memory.The maximum size of a cluster is hardware dependent and varies between devices.</p> </blockquote> <p>指定簇会将相邻的线程块分组到各个簇中，并在簇级别提供一些额外的同步与通信机会。具体而言，一个簇中的所有线程块都在<strong>同一个 GPC 内执行</strong>。<a href="https://docs.nvidia.com/cuda/cuda-programming-guide/01-introduction/programming-model.html#thread-block-scheduling-with-clusters">https://docs.nvidia.com/cuda/cuda-programming-guide/01-introduction/programming-model.html#thread-block-scheduling-with-clusters</a> 展示了指定簇时，线程块如何被调度到一个 GPC 内的各个 SM 上。由于这些线程块是同时被调度的，且位于同一个 GPC 内，因此<strong>不同 block 但属于同一簇的线程</strong>可以通过 <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/writing-cuda-kernels.html#writing-cuda-kernels-cooperative-groups">https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/writing-cuda-kernels.html#writing-cuda-kernels-cooperative-groups</a> 提供的软件接口相互通信和同步。簇中的线程可以访问该簇内所有 block 的共享内存，这被称为<strong>分布式共享内存（distributed shared memory）</strong>（<a href="https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/writing-cuda-kernels.html#writing-cuda-kernels-distributed-shared-memory">https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/writing-cuda-kernels.html#writing-cuda-kernels-distributed-shared-memory</a>）。簇的最大大小取决于硬件，因设备而异。</p> <blockquote> <p>Figure 6 illustrates the how thread blocks within a cluster are scheduled simultaneously on SMs within a GPC. Thread blocks within a cluster are always adjacent to each other within the grid.</p> </blockquote> <p>图 6 展示了一个簇内的线程块如何同时被调度到一个 GPC 内的各个 SM 上。一个簇内的线程块在网格中总是彼此相邻的。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/cuda101/image06-480.webp 480w,/assets/img/cuda101/image06-800.webp 800w,/assets/img/cuda101/image06-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/cuda101/image06.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <div class="caption"> 图 6：当指定簇时，一个簇内的线程块被同时调度到一个 GPC 内的各个 SM 上。一个簇内的线程块在网格中总是彼此相邻的。 </div> <h3 id="1222-warps-and-simt">1.2.2.2. Warps and SIMT</h3> <blockquote> <p>Within a thread block, threads are organized into groups of 32 threads called warps. A warp executes the kernel code in a Single-Instruction Multiple-Threads (SIMT) paradigm. In SIMT, all threads in the warp are executing the same kernel code, but each thread may follow different branches through the code. That is, though all threads of the program execute the same code, threads do not need to follow the same execution path.</p> </blockquote> <p>在线程块内部，线程被组织成由 32 个线程组成的组，称为<strong>线程束（warp）</strong>。一个 warp 以<strong>单指令多线程（Single-Instruction Multiple-Threads，简称 SIMT）</strong>范式执行内核代码。在 SIMT 中，warp 内的所有线程执行同一份内核代码，但每个线程可能在代码中走不同的分支。也就是说，尽管程序的所有线程执行同一份代码，但线程不必遵循相同的执行路径。</p> <blockquote> <p>When threads are executed by a warp, they are assigned a warp lane. Warp lanes are numbered 0 to 31 and threads from a thread block are assigned to warps in a predictable fashion detailed in Hardware Multithreading.</p> </blockquote> <p>当线程由一个 warp 执行时，它们会被分配一个 <strong>warp lane（warp 通道）</strong>。Warp lane 的编号为 0 到 31，线程块中的线程以一种可预测的方式被分配到各个 warp 中，具体方式详见 “硬件多线程（Hardware Multithreading）” 章节。</p> <blockquote> <p>All threads in the warp execute the same instruction simultaneously. If some threads within a warp follow a control flow branch in execution while others do not, the threads which do not follow the branch will be masked off while the threads which follow the branch are executed. For example, if a conditional is only true for half the threads in a warp, the other half of the warp would be masked off while the active threads execute those instructions. This situation is illustrated in Figure 7. When different threads in a warp follow different code paths, this is sometimes called warp divergence. It follows that utilization of the GPU is maximized when threads within a warp follow the same control flow path.</p> </blockquote> <p>Warp 中的所有线程同时执行同一条指令。如果 warp 内的某些线程在执行中走了某个控制流分支，而其他线程没有，那么在执行该分支的指令时，没有走该分支的线程会被<strong>屏蔽（masked off）</strong>。例如，如果一个条件只对 warp 中一半的线程为真，那么在活跃线程执行这些指令时，warp 的另一半会被屏蔽。这种情况如 图 7 所示。当 warp 内的不同线程走不同的代码路径时，这种情况有时被称为 <strong>warp 发散（warp divergence）</strong>。由此可知，当 warp 内的线程走相同的控制流路径时，GPU 的利用率最高。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/cuda101/image07-480.webp 480w,/assets/img/cuda101/image07-800.webp 800w,/assets/img/cuda101/image07-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/cuda101/image07.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <div class="caption"> 图 7：在本示例中，只有线程索引为偶数的线程执行 if 语句的主体，在执行该主体时，其余线程被屏蔽。 </div> <blockquote> <p>In the SIMT model, all threads in a warp progress through the kernel in lock step. Hardware execution may differ. See the sections on Independent Thread Execution for more information on where this distinction is important. Exploiting knowledge of how warp execution is actually mapped to real hardware is discouraged. The CUDA programming model and SIMT say that all threads in a warp progress through the code together. Hardware may optimize masked lanes in ways that are transparent to the program so long as the programming model is followed. If the program violates this model, this can result in undefined behavior that can be different in different GPU hardware.</p> </blockquote> <p>在 SIMT 模型中，warp 内的所有线程以<strong>锁步（lock step）</strong>方式推进内核的执行。硬件的实际执行方式可能与此不同。关于这一区别在哪些情况下很重要，更多信息请参见 <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/03-advanced/advanced-kernel-programming.html#advanced-kernels-independent-thread-scheduling">https://docs.nvidia.com/cuda/cuda-programming-guide/03-advanced/advanced-kernel-programming.html#advanced-kernels-independent-thread-scheduling</a> 章节。<strong>不建议</strong>利用 “warp 执行实际上如何映射到真实硬件” 的知识来编程。CUDA 编程模型和 SIMT 规定：warp 内的所有线程共同推进代码执行。只要遵循编程模型，硬件可能以对程序透明的方式优化被屏蔽的 lane。如果程序违反了这一模型，可能导致<strong>未定义行为（undefined behavior）</strong>，且在不同的 GPU 硬件上表现可能不同。</p> <blockquote> <p>While it is not necessary to consider warps when writing CUDA code, understanding the warp execution model is helpful in understanding concepts such as global memory coalescing and shared memory bank access patterns. Some advanced programming techniques use specialization of warps within a thread block to limit thread divergence and maximize utilization. This and other optimizations make use of the knowledge that threads are grouped into warps when executing.</p> </blockquote> <p>虽然编写 CUDA 代码时<strong>不必</strong>考虑 warp，但理解 warp 执行模型有助于理解诸如 <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/writing-cuda-kernels.html#writing-cuda-kernels-coalesced-global-memory-access">https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/writing-cuda-kernels.html#writing-cuda-kernels-coalesced-global-memory-access</a> （合并的全局内存访问）和 <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/writing-cuda-kernels.html#writing-cuda-kernels-shared-memory-access-patterns">https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/writing-cuda-kernels.html#writing-cuda-kernels-shared-memory-access-patterns</a> （共享内存访问模式）等概念。一些高级编程技术会在线程块内对 warp 进行<strong>专门化（specialization）</strong>，以限制线程发散并最大化利用率。这类优化以及其他优化都利用了 “线程在执行时被分组为 warp” 这一知识。</p> <blockquote> <p>One implication of warp execution is that thread blocks are best specified to have a total number of threads which is a multiple of 32. It is legal to use any number of threads, but when the total is not a multiple of 32, the last warp of the thread block will have some lanes that are unused throughout execution. This will likely lead to suboptimal functional units utilization and memory access for that warp.</p> </blockquote> <p>Warp 执行的一个推论是：线程块的总线程数<strong>最好指定为 32 的倍数</strong>。使用任意数量的线程都是合法的，但当总数不是 32 的倍数时，线程块的最后一个 warp 会有一些 lane 在整个执行过程中未被使用。这可能导致该 warp 的功能单元利用率和内存访问效率不佳。</p> <blockquote> <p>SIMT is often compared to Single Instruction Multiple Data (SIMD) parallelism, but there are some important differences. In SIMD, execution follows a single control flow path, while in SIMT, each thread is allowed to follow its own control flow path. Because of this, SIMT does not have a fixed data-width like SIMD. A more detailed discussion of SIMT can be found in SIMT Execution Model.</p> </blockquote> <p>SIMT 常被拿来与<strong>单指令多数据（SIMD）</strong>并行相比较，但二者存在一些重要区别。在 SIMD 中，执行遵循<strong>单一控制流路径</strong>；而在 SIMT 中，每个线程都被允许走<strong>自己的控制流路径</strong>。正因如此，SIMT 不像 SIMD 那样具有固定的数据宽度。关于 SIMT 更详细的讨论可参见 <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/03-advanced/advanced-kernel-programming.html#advanced-kernels-hardware-implementation-simt-architecture">https://docs.nvidia.com/cuda/cuda-programming-guide/03-advanced/advanced-kernel-programming.html#advanced-kernels-hardware-implementation-simt-architecture</a> 。</p> <h3 id="1223-tile-programming-in-cuda">1.2.2.3. Tile Programming in CUDA</h3>]]></content><author><name></name></author><category term="cuda"/><summary type="html"><![CDATA[《CUDA C++ Programming Guide》13.3 第 1 章 Introduction 1.2.1–1.2.3 节的中英对照翻译。]]></summary></entry><entry><title type="html">我所理解的 CUDA · 开篇</title><link href="https://chriszhao13.github.io/blog/2026/cuda-understanding-01/" rel="alternate" type="text/html" title="我所理解的 CUDA · 开篇"/><published>2026-08-11T02:00:00+00:00</published><updated>2026-08-11T02:00:00+00:00</updated><id>https://chriszhao13.github.io/blog/2026/cuda-understanding-01</id><content type="html" xml:base="https://chriszhao13.github.io/blog/2026/cuda-understanding-01/"><![CDATA[<blockquote> <p>本系列以 NVIDIA 官方 <strong><a href="https://docs.nvidia.com/cuda/cuda-programming-guide/contents.html">《CUDA C++ Programming Guide》Release 13.3</a></strong>（2026 年 5 月发布）为唯一权威依据，用中文逐步解读：先从基本原理建立模型，再走到最新特性，配合可执行的代码示例加深理解。</p> </blockquote> <h2 id="为什么以-programming-guide-为纲">为什么以 Programming Guide 为纲</h2> <p>GPU 与 CUDA 的资料浩如烟海，博客、教程、白皮书各有侧重，但<strong>官方 Programming Guide 是唯一一份从编程模型一路讲到硬件实现、且全链路自洽</strong>的文档。作为 GPU 加速计算领域的研究人员，它迟早是要完整走一遍的。这个系列便为此而写——以写促学，逼着自己把每一章真正读懂、想透，而不是浅尝辄止。</p> <p>选择它，还因为它有几点不可替代：</p> <ul> <li><strong>概念稳定</strong>——它讲的是基本概念（线程层次、内存层次、SIMT），这些几乎不随架构迭代而变化；</li> <li><strong>特性最新</strong>——它同步跟踪最新特性（13.3 已覆盖 Thread Block Clusters、分布式共享内存、CUDA Graphs 条件节点、设备端图启动等）；</li> <li><strong>可作坐标</strong>——它是理解其他一切资料（Best Practices、架构白皮书、技术博客）的坐标系。</li> </ul> <h2 id="系列规划">系列规划</h2> <table> <thead> <tr> <th>篇章</th> <th>主题</th> </tr> </thead> <tbody> <tr> <td>开篇</td> <td>为什么跟着 Programming Guide 学 CUDA（本篇）</td> </tr> <tr> <td>第 1 章</td> <td>Chapter 1. Introduction to CUDA</td> </tr> </tbody> </table> <hr/> <p><em>这是用来给我自己学习的记录，如果理解有误欢迎讨论。</em></p>]]></content><author><name></name></author><category term="cuda"/><summary type="html"><![CDATA[一个以 CUDA C++ Programming Guide Release 13.3 为纲，从基本原理到最新特性逐步深入的 CUDA 学习系列。]]></summary></entry><entry><title type="html">我所理解的 CUDA · 第 1 章：Introduction</title><link href="https://chriszhao13.github.io/blog/2026/introduction-to-cuda/" rel="alternate" type="text/html" title="我所理解的 CUDA · 第 1 章：Introduction"/><published>2026-08-11T02:00:00+00:00</published><updated>2026-08-11T02:00:00+00:00</updated><id>https://chriszhao13.github.io/blog/2026/introduction-to-cuda</id><content type="html" xml:base="https://chriszhao13.github.io/blog/2026/introduction-to-cuda/"><![CDATA[<blockquote> <p>本节对应《CUDA C++ Programming Guide》Release 13.3 <strong>第 1 章 Introduction，§1.1.1 The Graphics Processing Unit</strong>，采用中英对照。</p> </blockquote> <h2 id="111-the-graphics-processing-unit图形处理单元">1.1.1 The Graphics Processing Unit（图形处理单元）</h2> <blockquote> <p>Born as a special-purpose processor for 3D graphics, the Graphics Processing Unit (GPU) started out as fixed-function hardware to accelerate parallel operations in real-time 3D rendering. Over successive generations, GPUs became more programmable. By 2003, some stages of the graphics pipeline became fully programmable, running custom code in parallel for each component of a 3D scene or an image.</p> </blockquote> <p>图形处理单元（Graphics Processing Unit，GPU）诞生之初是为 3D 图形而生的专用处理器，最初以固定功能（fixed-function）硬件的形式，加速实时 3D 渲染中的并行操作。随着一代又一代的演进，GPU 变得越来越可编程。到 2003 年，图形管线中的某些阶段已经可以完全可编程，能够针对 3D 场景或图像中的每一个分量并行执行自定义代码。</p> <blockquote> <p>In 2006, NVIDIA introduced the Compute Unified Device Architecture (CUDA) to enable any computational workload to use the throughput capability of GPUs independent of graphics APIs.</p> </blockquote> <p>2006 年，NVIDIA 推出了计算统一设备架构（Compute Unified Device Architecture，CUDA），使任何计算负载都能独立于图形 API，利用 GPU 的吞吐能力（throughput capability）。</p> <blockquote> <p>Since then, CUDA and GPU computing have been used to accelerate computational workloads of nearly every type, from scientific simulations such as fluid dynamics or energy transport to business applications like databases and analytics. Moreover, the capability and programmability of GPUs has been foundational to the advancement of new algorithms and technologies ranging from image classification to generative artificial intelligence such as diffusion or large language models.</p> </blockquote> <p>自此以后，CUDA 与 GPU 计算被用于加速几乎所有类型的计算负载：从流体动力学、能量输运等科学模拟，到数据库、数据分析等商业应用。此外，GPU 的能力与可编程性，已成为从图像分类到生成式人工智能（如扩散模型、大语言模型）等新算法与新技术进步的基础（foundational）。</p> <hr/> <blockquote> <p>本节对应《CUDA C++ Programming Guide》Release 13.3 <strong>第 1 章 Introduction，§1.1.2 The Benefits of Using GPUs</strong>，采用中英对照。</p> </blockquote> <h2 id="112-the-benefits-of-using-gpus使用-gpu-的收益">1.1.2 The Benefits of Using GPUs（使用 GPU 的收益）</h2> <blockquote> <p>A GPU provides much higher instruction throughput and memory bandwidth than a CPU within a similar price and power envelope. Many applications leverage these capabilities to run significantly faster on the GPU than on the CPU (see <a href="https://www.nvidia.com/en-us/accelerated-applications/">GPU Applications</a>). Other computing devices, like FPGAs, are also very energy efficient, but offer much less programming flexibility than GPUs.</p> </blockquote> <p>GPU 在相近的价格与功耗范围（price and power envelope）内，能提供比 CPU 高得多的指令吞吐量（instruction throughput）与内存带宽（memory bandwidth）。许多应用利用这些能力，在 GPU 上比在 CPU 上运行得明显更快（参见 <a href="https://www.nvidia.com/en-us/accelerated-applications/">GPU Applications</a>）。其他计算设备，如 FPGA，也非常节能，但相比 GPU，其编程灵活性（programming flexibility）要低得多。</p> <blockquote> <p>GPUs and CPUs are designed with different goals in mind. While a CPU is designed to excel at executing a serial sequence of operations (called a thread) as fast as possible and can execute a few tens of these threads in parallel, a GPU is designed to excel at executing thousands of threads in parallel, trading off lower single-thread performance to achieve much greater total throughput.</p> </blockquote> <p>GPU 与 CPU 的设计目标各不相同。CPU 的设计目标是尽可能快地执行串行操作序列（称为线程 thread），并能并行执行数十个这样的线程；而 GPU 的设计目标则是并行执行数千个线程，以较低的单线程性能（single-thread performance）为代价，换取大得多的总吞吐量（total throughput）。</p> <blockquote> <p>GPUs are specialized for highly parallel computations and devote more transistors to data processing units, while CPUs dedicate more transistors to data caching and flow control. <a href="https://docs.nvidia.com/cuda/cuda-programming-guide/01-introduction/introduction.html#from-graphics-processing-to-general-purpose-parallel-computing-gpu-devotes-more-transistors-to-data-processing">Figure 1</a> shows an example distribution of chip resources for a CPU versus a GPU.</p> </blockquote> <p>GPU 专精于高度并行的计算，将更多晶体管（transistors）投入到数据处理单元；而 CPU 则将更多晶体管用于数据缓存（data caching）与流控制（flow control）。<a href="https://docs.nvidia.com/cuda/cuda-programming-guide/01-introduction/introduction.html#from-graphics-processing-to-general-purpose-parallel-computing-gpu-devotes-more-transistors-to-data-processing">图 1</a> 展示了 CPU 与 GPU 芯片资源分布的一个示例。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/cuda101/image01-480.webp 480w,/assets/img/cuda101/image01-800.webp 800w,/assets/img/cuda101/image01-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/cuda101/image01.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <div class="caption"> 图 1：GPU 将更多晶体管投入到数据处理 </div> <hr/> <blockquote> <p>本节对应《CUDA C++ Programming Guide》Release 13.3 <strong>第 1 章 Introduction，§1.1.3 Getting Started Quickly</strong>，采用中英对照。</p> </blockquote> <h2 id="113-getting-started-quickly快速上手">1.1.3 Getting Started Quickly（快速上手）</h2> <blockquote> <p>There are many ways to leverage the compute power provided by GPUs. This guide covers programming for the CUDA GPU platform in high-level languages such as C++. However, there are many ways to utilize GPUs in applications that do not require directly writing GPU code.</p> </blockquote> <p>利用 GPU 提供的计算能力有多种方式。本指南介绍的是用 C++ 等高级语言为 CUDA GPU 平台编程的方法。不过，在不直接编写 GPU 代码的情况下，也有很多方式可以在应用中使用 GPU。</p> <blockquote> <p>An ever-growing collection of algorithms and routines from a variety of domains is available through specialized libraries. When a library has already been implemented—especially those provided by NVIDIA—using it is often more productive and performant than reimplementing algorithms from scratch. Libraries like cuBLAS, cuFFT, cuDNN, and CUTLASS are just a few examples of libraries that help developers avoid reimplementing well-established algorithms. These libraries have the added benefit of being optimized for each GPU architecture, providing an ideal mix of productivity, performance, and portability.</p> </blockquote> <p>来自众多领域、且不断增长的算法与例程（routines）（CZ: 这个是个固定术语）集合，可通过专用库（specialized libraries）获取。当某个库已经被实现出来时——尤其是 NVIDIA 提供的库——使用它往往比从头重新实现算法更高效、性能更好。cuBLAS、cuFFT、cuDNN 与 CUTLASS 等库，只是帮助开发者避免重复实现成熟算法的少数几个例子。这些库还有一项额外的好处：它们针对每一种 GPU 架构都做了优化，从而在生产效率（productivity）、性能（performance）与可移植性（portability）之间提供了理想的组合。</p> <blockquote> <p>There are also frameworks, particularly those used for artificial intelligence, that provide GPU-accelerated building blocks. Many of these frameworks achieve their acceleration by leveraging the GPU-accelerated libraries mentioned above.</p> </blockquote> <p>还有一些框架，尤其是用于人工智能的框架，提供 GPU 加速的构建模块（building blocks）。其中许多框架正是通过利用上述 GPU 加速库来实现其加速效果的。</p> <blockquote> <p>Additionally, domain-specific languages (DSLs) such as NVIDIA’s Warp or OpenAI’s Triton compile to run directly on the CUDA platform. This provides an even higher-level method of programming GPUs than the high-level languages covered in this guide.</p> </blockquote> <p>此外，诸如 NVIDIA 的 Warp、OpenAI 的 Triton 等领域特定语言（domain-specific languages，DSLs），会被编译为直接在 CUDA 平台上运行。与指南所涵盖的高级语言相比，这提供了一种更高级的 GPU 编程方式。</p> <blockquote> <p>The <a href="https://github.com/NVIDIA/accelerated-computing-hub">NVIDIA Accelerated Computing Hub</a> contains resources, examples, and tutorials to teach GPU and CUDA computing.</p> </blockquote> <p><a href="https://github.com/NVIDIA/accelerated-computing-hub">NVIDIA Accelerated Computing Hub</a> 收录了用于教授 GPU 与 CUDA 计算的资源、示例和教程。</p>]]></content><author><name></name></author><category term="cuda"/><summary type="html"><![CDATA[《CUDA C++ Programming Guide》13.3 第 1 章 Introduction 1.1.1–1.1.3 节的中英对照翻译。]]></summary></entry></feed>